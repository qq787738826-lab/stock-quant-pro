# 智能体团队架构决策

本文件冻结跨阶段决策。更改这些决策必须单独评审，不能作为普通实现细节顺带修改。

## 权威与边界

1. **Java 权威**：Java 唯一生成 taskId 和六个 runId，负责状态、幂等、缓存、持久化和跨语言结果校验。
2. **Python 无状态**：Python 只分析 Java 提供的只读 contextSnapshot，不访问任务数据库，不保存状态，不改写 Java 身份。
3. **六智能体固定名单**：只包含 DATA_QUALITY、MARKET_REGIME、TECHNICAL_ANALYSIS、STRATEGY_BACKTEST、ANNOUNCEMENT_RISK、POSITION_RISK。
4. **总控不是 agentRun**：不得创建 `CHIEF_DECISION` run；总控结果只进入 `agent_decisions`。

## 风险、证据与决策

5. **POSITION_RISK 唯一正式否决权**：其他智能体不得声明或创建正式 veto。
6. **DATA_QUALITY 只负责门禁阻断**：`BLOCKED` 不是正式否决，DATA_QUALITY 的 `veto` 必须为 false。
7. **证据优先于自然语言结论**：finding 和 veto 必须引用真实、完整、唯一的 evidence；不得编造证据。
8. **规则评分与 LLM 解释分离**：可审计规则产生结构化评分和决策；未来 LLM 最多作为解释层，不得成为权威计算或证据来源。

## 演进与治理

9. **先本地 PostgreSQL，后外部数据源**：优先复用已有数据表建立只读上下文，再评估外部数据源。
10. **先规则能力，后 LLM**：先完成可复现、可测试的本地规则能力，再单独评估模型解释层。
11. **不自动交易**：智能体输出仅用于研究和模拟，不直接修改现金、持仓、成交或控制券商客户端。
12. **大阶段隔离**：每个大阶段使用独立任务分支、完整测试、ChatGPT 实际提交验收和用户 merge 批准，不跨大阶段偷渡功能；同一大阶段的内部工作包不分别提交或验收。
13. **单一事实来源**：`docs/agent-team/CURRENT_STATE.md` 是进度、能力边界和下一阶段入口的唯一权威状态文件。

## 证券状态事件物化

14. **稳定证券身份必须显式映射**：跨来源证券身份必须通过 `security_identity_registry` 与 `source_security_identity_mappings` 的显式关系解析；`sourceInstrumentId` 缺失时不得物化事件。mappingLogicalKey 只使用稳定业务字段，mapping append-only 且 TEST/DEMO 隔离，mappingAssuranceLevel 参与 event assurance 保守传播；同 key 不同 identity 必须 conflict。symbol、名称、代码前缀、board 或 exchange 均不得被用来自动合并或修复身份。
15. **active 表示上市资格而非当日可交易性**：`active` 表示证券仍具有目标市场上市与 Universe 资格。普通停牌、临时停牌、无成交或缺 bar 不改变 `active`；`active=true` 必须满足 `listed=true`，退市结果必须为 `listed=false, active=false`。
16. **V1 事件转换不可放宽**：`SECURITY_STATUS_EVENT_V1` 继续复用现有完整 `resultingState` 和七类事件转换语义。初始链只能使用 `FULL_STATUS_SNAPSHOT`；增量事件只能改变冻结目标字段；多字段、身份或局部 valid 更正不得拆成多个 V1 事件。
17. **raw、attempt、result 与 event 基数固定**：一条 raw revision 可以被多个 run 处理；每个 run/raw 可有连续的多个 terminal attempt，每个 attempt 恰好一个 result，每个 result 引用零或一个 event。相同 eventLogicalKey 全局最多一个 event 和一条 lineage；result 唯一边界是 attempt，不得错误地对 raw 建立全局唯一。AttemptStatus 与 normalization outcome 必须精确映射，`REJECTED` 不得替代 identity、contract 或 conflict 终态。
18. **V6 event 主表保持唯一权威**：`security_status_events` 是唯一证券状态 event 主表；eventContractVersion 必须持久化，来源 revision 唯一键必须包含 recordNamespace。同一 namespace、securityLogicalKey、contract 最多一个 FULL 根，非根必须引用同链 predecessor，且一个 predecessor 最多一个后继；direct SQL 不能构造双根或跨 namespace、identity、contract 链。业务 Hash 禁止使用数据库物理 ID。
19. **Event lineage 与 Manifest V2 独立版本化**：每个首次物化的逻辑 event 恰好一条 append-only raw→event lineage；复用 event 只新增本次 normalization result。`INGESTION_MANIFEST_V2_SECURITY_EVENT` 在 run 创建时冻结，覆盖该 run 全部 attempt/result；accepted/rejected 只取每条 raw 的最大连续 attemptNo，历史失败仍进入 manifest 但不重复计数。不能修改 `INGESTION_MANIFEST_V1` 既有编码或在封存时切换版本。
20. **事件时间与原子性**：event `knownAt` 必须等于 terminal attempt 的 `derivedKnownFrom`，effective、published、observed、recorded 与 known 时间不得混用，`completedAt` 不进入业务 manifest。event、首次 lineage、attempt 与 normalization result 必须在数据库事务中原子提交，并由唯一约束、不可变 trigger 和锁共同保护。

上述决策的完整字段、outcome、并发与验收契约见 [stage-2d2b1b-security-event-materialization-design.md](stage-2d2b1b-security-event-materialization-design.md)。它们只批准 TEST/DEMO 边界，不解除 FORMAL/PIT 门禁；2D-2B-1B-1 的实际完成状态继续以 [CURRENT_STATE.md](CURRENT_STATE.md) 为准。

## 阶段开发治理

21. **权威文档职责固定**：根目录 [AGENTS.md](../../AGENTS.md) 约束开发执行和安全；`CURRENT_STATE.md` 是当前进度、能力、阻断和入口的唯一事实来源；本文件冻结跨阶段决策；`ROADMAP.md` 只定义方向、顺序、依赖和验收门槛。`PROGRESS_LOG.md` 只能索引已经完成、已经验收或已经合入的历史里程碑及其提交、测试、ChatGPT 验收与用户 merge 批准证据，不得记录活动状态、当前阻断、当前入口、下一阶段或当前工作任务。`tasks/` 只保存大阶段或其内部工作包的任务边界。二者都不得成为平行状态权威。
22. **双窗口开发模式**：ChatGPT 窗口规划较大的完整开发阶段，定义架构、范围、边界和验收标准，并在 Codex 推送后基于实际 Git commit、完整 diff 和测试证据验收；Codex 开发窗口在一个任务分支连续完成该大阶段全部内部工作包，自主实现、测试、修复、commit 和普通 push，但不得 merge 或自行进入下一大阶段。用户只需对高风险业务选择和重大方案分歧作出最终选择，并最终批准 merge，不需要逐文件批准或批准普通 commit/push。详细执行规则以 [AGENTS.md](../../AGENTS.md) 为准。
23. **实际提交验收**：Codex 的聊天汇报、未提交工作区和自审不能代替最终验收。ChatGPT 必须检查已推送任务分支的实际 commit SHA、完整提交差异、测试证据、阶段边界和权威文档一致性，并向用户报告通过或不通过；不通过时由同一 Codex 开发窗口继续修复并提交推送。项目不固定设置第三个独立 Codex Review 窗口。
24. **大阶段连续交付**：路线图不构成自动开发授权。每次授权一个较大的完整开发阶段，大阶段可包含多个内部工作包；内部工作包不分别开发、Review、提交或验收，Codex 应连续完成，最终形成大阶段任务分支提交。ChatGPT 验收通过后由用户批准 merge；合并后 Codex 停止，ChatGPT 才规划下一大阶段。
25. **自主修复与高风险暂停**：普通编译错误、测试失败、局部重构、文件数量、常规实现选择和文档一致性问题由 Codex 在已授权大阶段内自主解决。仅当核心架构存在重大方案分歧、数据库核心模型或不可逆迁移、Java/Python 公共契约的重大选择、无法证明可消除的前视偏差、真实交易/下单/资金风险，或必须由用户选择的高风险业务规则时暂停。暂停报告必须说明原因、影响、方案、兼容性和迁移风险及所需选择；没有相应选择或新授权不得继续高风险实施。
26. **Canonical Hash 契约先于依赖实现**：任何大阶段新增会被持久化、进入公共契约或承担数据版本含义的领域 Hash 前，必须先在该大阶段架构中冻结版本化 canonical 契约。未冻结的算法、编码、Unicode、字段白名单、排序、空值、时间、精度、数据版本、跨语言和兼容策略不得由 Codex 猜测；如果形成重大公共契约、数据库模型或不可逆迁移选择，按第 25 条暂停。契约未冻结前不得实现依赖它的生产 Hash、迁移或公共契约。2F 内部工作包的具体门禁见 [tasks/2f0-backtest-context-foundation.md](tasks/2f0-backtest-context-foundation.md)。
27. **Knowledge-time 证据独立于业务日期与内容 Hash**：`trade_date<=requestTradeDate` 只证明业务日期上界，不证明该值在历史决策时点已经可知；内容 Hash 只证明给定内容稳定，也不证明历史可得性。可靠回测输入必须把决策时间、`knowledgeCutoff`、首次观察、来源/修订、快照或数据集版本、迟到与覆盖、公司行动和复权变化纳入可审计契约；无法证明 knowledge-time 合法性时 `backtestContext` 必须保持 `available=false`。2F 获准采用的不可变观察、PIT 模型、`known_at` 与 V9 迁移边界由下列决定冻结。

## 可靠回测与 STRATEGY_BACKTEST

28. **V9 本地 PIT 日线观察模型**：V9 新增 append-only `market_data_observation_batches` 与 `daily_bar_observations`，`daily_bars` 继续只是当前态兼容投影。观察批次和日线版本禁止 `UPDATE`、`DELETE`、`TRUNCATE`；同一来源、证券、交易日的 QFQ 内容和 revision 与当时最新观察相同时不重复追加，任一项相对最新观察变化时必须追加新版本，A→B→A 也保留后一次 A。持久化入口和数据库均拒绝周末日线；只有周一至周五且捕获时刻不早于该交易日上海时间 15:00 的完整日线才进入可靠 PIT 观察。工作日收盘前当日日线可以按原业务更新 `daily_bars`，但不得创建语义虚假的空观察批次或错误 `record_count`。成功的合格 PIT 捕获与当前态投影在同一事务内持久化；不得回填或伪造历史 `known_at`。
29. **2F 日终 knowledge-time 契约**：市场时区固定为 `Asia/Shanghai`；`decisionTime` 与 `knowledgeCutoff` 均为 `requestTradeDate` 当日最后一微秒。完整日线的最早合法知识时间固定为其 `tradeDate` 当日 15:00 上海时间；可靠观察必须是周一至周五，并满足 `firstObservedAt>=earliestDailyBarKnownAt`、`knownAt>=earliestDailyBarKnownAt`、`firstObservedAt<=knownAt<=recordedAt`、`tradeDate<=requestTradeDate` 与 `knownAt<=knowledgeCutoff`。周末日线直接拒绝；法定节假日仍由实际数据来源和后续正式日历能力治理。非交易请求日可使用此前最近有效交易日，但 cutoff 不移动。请求日期在未来或日终决策时点尚未到达时安全不可用。内容 Hash 不替代 knowledge-time、来源 revision 或数据版本证据。
30. **版本化上下文兼容边界**：只有团队规则版本 `1.4.0-stage-2f-strategy-backtest-v1` 显式选择 `AGENT_CONTEXT_2F_V1/BACKTEST_CONTEXT_V1`。旧 `create(symbol, tradeDate)` 入口及旧规则版本继续生成既有 `backtestContext.available=false`，不得改变 2B、2D-1、2E-1 的 contextSnapshot、contextHash、缓存键或结果。
31. **BACKTEST_CANONICAL_V1**：三个领域 Hash 固定使用 SHA-256、小写十六进制、UTF-8、Unicode NFC、对象字段名词典序、数组契约业务顺序、UTC `Z` 微秒时间及规范化十进制普通字符串；缺失字段与显式 `null` 不等价。`inputDataHash`、`strategyDefinitionHash`、`backtestResultHash` 各自只覆盖冻结白名单，数据库 ID、创建时间、日志与运行环境噪声不得进入。`dataVersion` 是独立的 PIT 模型、batch/dataset、所选观察版本和 source revision 对象，不得以 `inputDataHash` 冒充。
32. **冻结回测策略事实**：策略、引擎和参数版本分别为 `SMA20_NEXT_OPEN_RISK_EXIT_V1`、`BACKTEST_ENGINE_V1`、`BACKTEST_PARAMS_V1`。七项参数必须完整显式持久化；Java 是回测执行、稳定子区间和生产 Hash 的权威方。Python 不拉取行情、不访问数据库、不重跑策略或寻优，只按相同 canonical 契约校验并解释 Java 事实。
33. **STRATEGY_BACKTEST V1 安全边界**：可靠输入固定生成样本充分性、总收益、最大回撤、胜率与盈亏比、跨时间子区间稳定性五类 finding，并按冻结阈值计算 `[0,100]` score 与最高 80 的 confidence。DATA_QUALITY 阻断、上下文不可用、输入非法或交易样本不足均不得生成正常性能评分。STRATEGY_BACKTEST 永不产生正式 veto、投资建议、交易指令或收益承诺；POSITION_RISK 仍是唯一正式否决权，总控在公告风险与仓位风险未实现时仍保持安全不足结论。

## 可靠模拟持仓与 POSITION_RISK

34. **2H 当前模拟账户快照边界**：只有团队规则版本 `1.4.0-stage-2h-position-risk-v1` 选择 `AGENT_CONTEXT_2H_V1/PORTFOLIO_CONTEXT_V1`，且只读取默认模拟账户 `accountId=1`。Java 必须在同一个 `REPEATABLE_READ`、`readOnly=true` 事务中通过 Agent 专用 Repository 冻结账户、持仓、`PENDING_CONFIRM` 委托、请求日前权益快照、组合设置和请求日及以前最近本地 QFQ 收盘价；不得调用会结算、刷新行情、保存快照或写风险事件的 PortfolioService 路径。`requestTradeDate` 必须等于上下文生成时的上海自然日，周末可以评估当前状态；历史或未来日期安全不可用。
35. **portfolioContext 不提供历史 PIT 承诺**：2H V1 固定声明 `currentStateOnly=true`、`snapshotFrozenForTask=true`、`historicalPointInTimeGuaranteed=false`、`businessTablesReadOnly=true`。持仓缺少本地 QFQ 估值价或估值价超过七个自然日时整个上下文不可用；四至七日价格只降低 score/confidence。资金、市场价值、未实现损益、仓位权重、待买暴露和预计仓位均由 Java 从冻结明细重算并交叉验证。缺少历史权益指标不使上下文整体不可用，但 POSITION_RISK 必须进入 `PARTIAL`，按缺失项降低 confidence；无正式 veto 时 gate 为 `WARN`，存在正式 veto 时 gate 为 `BLOCKED`。该阶段不新增迁移，也不得伪造历史持仓版本。
36. **POSITION_RISK V1 正式否决顺序**：正式 veto 只可由 POSITION_RISK 按账户回撤、当日损失、最大持仓数、当前仓位权重、待买后预计权重、止损、移动止损的固定顺序生成；各 symbol 组内部按 symbol 升序。symbol 级 vetoCode 带稳定 symbol 后缀以满足既有 V5 唯一键；vetoId、reason、阈值、scope 和唯一 `PORTFOLIO_STATE` evidence 引用必须稳定。目标价达到只形成 warning finding，不得产生正式 veto；任何输出都不得成为交易执行指令。
37. **2H 总控优先级与兼容边界**：有效 portfolioContext 即使 DATA_QUALITY 为 `BLOCKED` 也必须执行 POSITION_RISK。存在正式 veto 时，总控固定为 `REJECTED_BY_VETO/BLOCKED/vetoed=true/score=0`，且完整引用全部 vetoId；无 veto 时 DATA_QUALITY 阻断仍形成 `BLOCKED_BY_DATA_QUALITY`；两者均未阻断时，因 ANNOUNCEMENT_RISK 尚未实现而保持 `INSUFFICIENT_DATA`。2H 不改变六个固定 run、总控非第七 run、旧 2B/2D-1/2E-1/2F profile、contextHash、缓存键或既有规则语义。

## 研究级公告事实与 ANNOUNCEMENT_RISK

38. **AKShare/CNINFO 只具有研究来源资格**：研究 Provider 固定为 `AKSHARE_CNINFO_RESEARCH_V1/AKSHARE_CNINFO_PROVIDER_V1`，AKShare 版本固定为 `1.18.64`。该来源的 assurance 只能是 `RESEARCH`，并固定 `formalEligible=false`、`pitVerified=false`、`revisionRelationshipGuaranteed=false`、`reportedPublishTimePrecision=DATE_ONLY`。它可以支持个人研究、风险提示、规则开发和从真实观察时点起的审计积累，但不能声明正式授权、历史绝对完整、精确首次公开时间、完整修订关系或自动交易资格，也不能解除 2D 正式证券状态来源和许可门禁。Python Provider Bridge 只调用 AKShare 公开函数并返回规范化原始记录；Java 仍是 observed/known time、校验、Hash、持久化和 Agent context 的唯一权威，Agent Rule Engine 不得调用 Provider、数据库、PDF 或外部网络。
39. **V10 append-only 公告观察模型与 knowledge-time**：V10 只新增不可变 `announcement_capture_batches` 与 `announcement_observations`，数据库触发器禁止 `UPDATE`、`DELETE`、`TRUNCATE`。完整成功且 0 条记录的批次是有效覆盖证据；部分失败批次只能审计，不能证明范围内无公告。公告发布时间只保存日期，不伪造日内时刻；Java 完整收到并验证 Provider 响应后冻结 `firstObservedAt`，且 `knownAt=firstObservedAt<=recordedAt`。历史公告今天首次观察时只能从今天的 knowledge-time 起可见。同一逻辑公告与最新版本 canonical 内容相同时幂等，内容变化及 A→B→A 都追加观察版本，不覆盖旧事实。
40. **公告来源身份与 ANNOUNCEMENT_CANONICAL_V1**：来源公告身份优先使用 CNINFO 明确公告 ID，编码为 `CNINFO:<id>`；没有明确 ID 时只允许对规范化 HTTP/HTTPS URL 生成 `CNINFO_URL_SHA256:<sha256>` 并标记 `URL_DERIVED`，禁止由标题、日期、symbol 或抓取顺序生成身份。`AKSHARE_CNINFO_RESEARCH_V1` 的 URL host 只允许 `cninfo.com.cn` 或其真实子域，拒绝相似后缀、userinfo 和非默认端口；该门禁由 Provider、Java canonical/摄取/context/响应校验、Python 规则输入校验和 V10 数据库共同执行。`ANNOUNCEMENT_CANONICAL_V1` 对来源、身份强度、证券、名称、标题、报告日期、日期精度、规范化 URL 和 assurance flags 使用冻结白名单、UTF-8 与 SHA-256 小写十六进制；Java 生成生产 Hash 和 observationVersion，Java/Python 通过固定输入、canonical 文本与外部固定预期值的黄金向量交叉验证。
41. **AGENT_CONTEXT_2G_V1 与公告覆盖资格**：团队规则版本 `1.4.0-stage-2g-announcement-risk-v1` 选择 `AGENT_CONTEXT_2G_V1/SECURITY_EVENTS_CONTEXT_V1`，同时复用 2F backtestContext 和 2H portfolioContext；精确 2I 规则版本只允许复用与其完全相同的组合上下文语义。旧 2B、2D-1、2E-1、2F、2H profile/contextHash 不变。securityEvents 固定回看 180 个自然日，只选择 `reportedPublishDate<=requestTradeDate` 且 `knownAt<=knowledgeCutoff` 的每个逻辑公告最新可见版本。可用性必须由同 symbol、正确来源/Provider、完整覆盖窗口、`complete=true`、不晚于 cutoff 且年龄不超过 24 小时的批次证明；完整 0 公告批次可以可用，无批次、过期、部分或范围不足必须安全不可用。研究来源、日期精度、无修订/历史完整性/PIT/正式资格和无 PDF 解析限制必须随 context 固定披露。
42. **ANNOUNCEMENT_RISK V1 是无正式否决权的确定性标题规则**：Python 只解释 Java 冻结的 securityEvents，并按 Unicode NFKC、空白/中文标点和拉丁字母规范化后的冻结关键词、排除和组合规则生成风险标签。排除语义是短语级：只从规则匹配副本中移除明确安全短语，再对剩余标题匹配正向关键词；不得因同一标题包含“撤销/解除”而全局消除其中仍存在的继续、新增风险。一条公告可以命中多个标签，但只按最高 `CRITICAL/HIGH/WARN/INFO` severity 扣分一次；基础扣分为 40/25/10/0，按 0–7、8–30、31–90、91–180 天乘以 1/0.75/0.50/0.25 并 `HALF_UP` 到整数，最终 score 截断至 `[0,100]`，confidence 固定为 40。有效上下文固定生成五类 finding、一个 coverage evidence 和每个命中公告的 event evidence；DATA_QUALITY 阻断、上下文不可用或输入非法不得生成正常评分。ANNOUNCEMENT_RISK 永不产生正式 veto、交易指令、投资建议或收益承诺。
43. **2G 六 run 与总控边界**：2G 保持 DATA_QUALITY、MARKET_REGIME、TECHNICAL_ANALYSIS、STRATEGY_BACKTEST、ANNOUNCEMENT_RISK、POSITION_RISK 六个专业 run，总控不是第七个 run。POSITION_RISK 正式 veto 最高优先并形成 `REJECTED_BY_VETO`；没有 veto 时 DATA_QUALITY 阻断形成 `BLOCKED_BY_DATA_QUALITY`；两者均不存在时六个专业 run 虽已执行，但 2G 冻结规则版本不包含后来新增的 2I 综合决策，总控仍固定为 `INSUFFICIENT_DATA/score=0/confidence=0`。后来合入 2I 不得静默升级旧 2G 规则版本。2G 不提前实现 RESEARCH_ONLY、WATCH、PASS_TO_MANUAL_REVIEW、综合评分或投资结论。

## 确定性总控综合决策

44. **2I 只新增版本化总控，不新增上下文或第七个 run**：团队规则版本固定为 `1.4.0-stage-2i-chief-decision-v1`，总控契约为 `CHIEF_DECISION_V1`，权重契约为 `CHIEF_SCORE_WEIGHTS_V1`。精确 2I 规则版本复用与 2G 完全相同的 `AGENT_CONTEXT_2G_V1` 组合上下文；不得增加外层 contextSnapshot 字段、修改既有子 Context Schema、改变 2G contextHash、修改旧规则版本或创建 `CHIEF_DECISION` run。总控结果继续只进入 V5 `agent_decisions`，不新增 Flyway。
45. **2I 输入角色与固定权重**：DATA_QUALITY 只承担安全门禁、质量警告和综合 confidence 上限，其 score 不参与综合 score。MARKET_REGIME V1 的 score/confidence 权重均固定为 0，但必须处于当前 2D-1 契约允许的合法终态并继续进入 `sourceRunIds` 与顶层 findings/evidence。综合 score 和 confidence 只使用 TECHNICAL_ANALYSIS 25、STRATEGY_BACKTEST 35、ANNOUNCEMENT_RISK 20、POSITION_RISK 20，权重和精确为 100；不得按 confidence 重分配 score、在贡献者缺失时重新归一化或引入 finding/evidence 数量、summary 文本、错误文本、隐式指标或 LLM 输出。
46. **2I 决策优先级不可绕过**：任一合法 POSITION_RISK 正式 veto 最高优先，形成 `REJECTED_BY_VETO/BLOCKED/vetoed=true/score=0`，confidence 取 POSITION_RISK confidence；即使 DATA_QUALITY 阻断或其他 run 不足也不改变该优先级。无正式 veto 且 DATA_QUALITY 阻断时形成 `BLOCKED_BY_DATA_QUALITY/BLOCKED/vetoed=false/score=0`。前两者均不存在但 MARKET_REGIME、TECHNICAL_ANALYSIS、STRATEGY_BACKTEST、ANNOUNCEMENT_RISK 或 POSITION_RISK 任一不满足冻结可综合门禁时，必须形成 `INSUFFICIENT_DATA/NOT_APPLICABLE/0/0`，不得用残缺贡献者产生正常综合分类。非 POSITION_RISK veto 是非法响应。
47. **2I 综合计算与分类契约**：完整输入的 score 和原始 confidence 均按四个贡献者的固定权重求和后除以 100，并用非负整数 `HALF_UP` 取整。DATA_QUALITY 为 WARN 或 POSITION_RISK 为 PARTIAL 时，confidence 分别应用最高 50 的上限且分类最多为 `RESEARCH_ONLY`。公告与持仓风险只从冻结风险 finding code 的 severity 取 `INFO<WARN<HIGH<CRITICAL` 的最高值，不从 summary 重识别；公告来源覆盖和研究限制 finding 不等于发行人风险事件，不参与该最高风险值。DQ WARN、POSITION_RISK PARTIAL、风险 HIGH/CRITICAL、score<50 或 confidence<40 强制 `RESEARCH_ONLY`；只有全部人工复核门槛同时满足才能 `PASS_TO_MANUAL_REVIEW`，其余完整输入为 `WATCH`。`PASS_TO_MANUAL_REVIEW` 只表示进入人工研究复核。
48. **2I 输出只重用专业 findings/evidence**：`sourceRunIds` 必须按 DATA_QUALITY、MARKET_REGIME、TECHNICAL_ANALYSIS、STRATEGY_BACKTEST、ANNOUNCEMENT_RISK、POSITION_RISK 的固定顺序恰好包含六个 run；`vetoIds` 必须精确、稳定地等于顶层合法 POSITION_RISK veto 集合。final findings 与顶层 evidence 分别按相同六 run 顺序拼接，不创建总控 finding 或 evidence。summary 由决策、score、confidence 和固定研究边界模板生成，必须披露 MARKET_REGIME V1 权重为 0，且不得包含交易执行、仓位建议或收益承诺语言。
49. **Java 是 2I 最终复算与持久化权威**：Python 只执行无状态确定性总控规则，不访问数据库、网络，不修改事实或运行专业规则。Java 在持久化前独立复算六 run 身份、正式 veto/DQ/不足优先级、权重、`HALF_UP`、confidence 上限、最高风险 severity、finalDecision、gate、vetoed、score、confidence、summary、findings/evidence 顺序、sourceRunIds 与 vetoIds；任一不一致必须使 Agent 响应在既有事务边界内原子失败，不影响已经合法存在的行情、公告或模拟账户事实。2I 不改变 POSITION_RISK 唯一正式否决权，不产生投资建议、自动交易或真实账户能力。
50. **2I 专业 run 与总控持久化终态分层**：只对精确规则版本 `1.4.0-stage-2i-chief-decision-v1`，Java 必须显式依据 `finalDecision.decision` 决定总控终态，不得再只看专业 run 状态。`REJECTED_BY_VETO`、`BLOCKED_BY_DATA_QUALITY`、`RESEARCH_ONLY`、`WATCH`、`PASS_TO_MANUAL_REVIEW` 均表示确定性总控已经完成，task 与 decision 保存为 `COMPLETED` 并可进入既有完成缓存；只有最终 `INSUFFICIENT_DATA` 保存为 `task=PARTIAL/decision status=INSUFFICIENT_DATA` 且不得命中完成缓存。专业 run 的 `PARTIAL/INSUFFICIENT_DATA` 必须保留，旧 2B、2D-1、2E-1、2F、2G、2H 终态和缓存语义不变。

## 受控影子运行与就绪度观测

51. **3A-1 影子运行不改变 Agent 权威链**：影子控制契约固定为 `SHADOW_RUN_CONTROL_V1`，选择、结果快照、复核和指标契约分别固定为 `SHADOW_SELECTION_V1`、`SHADOW_OUTCOME_SNAPSHOT_V1`、`SHADOW_REVIEW_V1`、`SHADOW_METRICS_V1`。影子 item 必须通过既有 Java `AgentTaskService`、`TriggerType=SHADOW` 和精确 2I 规则版本创建真实 Agent 任务；不得直接调用 Python 绕过任务系统，不得直接写 `agent_runs` 或 `agent_decisions`，不得修改 Agent 结果、行情、公告、模拟账户或交易事实，也不得创建第七个 run。影子成功标准是可重复记录真实确定结果、真实不足和真实失败，不要求产生人工复核通过或收益表现。
52. **V11 冻结影子批次、item 与 append-only 复核事实**：V11 只新增 `agent_shadow_batches`、`agent_shadow_items`、`agent_shadow_reviews` 并把 `SHADOW` 加入既有 trigger type 数据库约束，不修改 V1 至 V10。batch/item 在运行期只允许生命周期字段按状态机推进；进入终态后，核心身份、symbol、Agent task 引用、final outcome、快照和漂移不得修改，终态批次不得重新进入运行态或追加 item。人工复核禁止 `UPDATE`、`DELETE`、`TRUNCATE`，更正只能追加新记录并引用 `supersedes_review_id`，且不能修改 Agent task 或 finalDecision。
53. **3A-1 选择、运行窗口和默认关闭契约**：EXPLICIT 只接受 1 至 20 个六位 symbol，去重后按 symbol 升序；AUTO 先选择当前模拟持仓并按 market value 降序、symbol 升序，再选择最新已完成扫描中的 eligible 候选并按 rank、symbol 升序，去重后按冻结 maxSymbols 截断。默认 maxSymbols 为 10、硬上限 20，不允许全市场遍历、随机选择、从 Python 取候选或从 Agent 结果反向扩充。影子功能和 scheduler 默认均关闭；scheduler 只有双开关开启、处于 `Asia/Shanghai` 安全窗口且无冲突运行时才可创建批次，不得自称具备正式交易日判断，不得自动打开配置。
54. **3A-1 结果、reason、漂移和指标只能来自结构化数据库事实**：`DETERMINED/INSUFFICIENT/FAILED/CANCELLED` 只根据真实 task、decision 和六 run 终态生成不可变 outcome snapshot。数据不足 reasonCode 只从六 run 的结构化 errors 按固定 run 顺序去重提取，不解析 summary；缺少结构化原因时使用 `SHADOW_INSUFFICIENT_WITHOUT_REASON_CODE`。漂移只比较同 symbol、同 ruleVersion 的最近上一条终态 item，覆盖 contextHash、finalDecision、score、confidence、六 run、veto 和 reasonCodes，不以收益或 summary 文本作为核心漂移。指标必须从数据库事实即时查询，不维护会漂移的手工计数缓存。
55. **3A-1 取消、熔断和长期 3A 边界**：取消只设置批次取消请求、停止启动新 item、等待已启动任务终结，并把未启动 item 标为 `CANCELLED`；不得强制终止已创建 Agent task 或删除结果。Python 连续不可达、item 连续创建失败、超过一半启动失败、数据库写失败或 runner 中断只熔断当前批次，不修改全局配置且不自动重试。3A-1 完成只表示受控技术基础可用；完整 3A 仍需不少于 20 个有效观察日、200 个 shadow item、主要 reasonCode 人工复核、持续业务表只读证明和正式观察报告，在此之前不得描述为 3A 完成或开始 3B。

## Flyway 迁移血统与测试隔离

56. **已执行迁移内容不可改写，合法差异只用前向迁移承接**：V6 的唯一合法仓库内容固定为提交 `39f929aadebf9e1df6c392d38b97d7058b17dfff` 中已被专用测试库执行的版本，Flyway checksum 为 `-981595186`。提交 `3a3eebd2ef580d31a6b02aab1a7204ea02fdba58` 后来追加的时态表不可变保护、knowledge-close 门禁及旧日历导航列删除不得继续保留在 V6，统一由 V12 前向承接。不得通过 repair、clean、baseline、关闭 validate、手工 checksum 或修改 `flyway_schema_history` 消除血统差异。
57. **V12 只承接 V6 的合法后续 Schema delta**：V7 至 V11 不依赖上述 delta 在 V7 前存在，因此不使用 V6.1、out-of-order 或重写 V7 至 V11。V12 删除 `trading_calendar_revisions.previous_open_date/next_open_date` 前必须在同一迁移事务中证明两列均无非空值；门禁不满足时整体失败并保留旧血统，不允许静默删值、补值或改写历史事实。
58. **数据库集成测试禁止向 public 执行 Flyway migrate**：任何会运行 migrate 的 PostgreSQL 测试必须创建随机隔离 Schema，并同时显式配置 datasource `currentSchema`、Hikari schema、Flyway `default-schema`、`schemas` 与 `create-schemas=false`；迁移目标为 `public` 或不符合随机 Schema 契约时必须在 migrate 前失败。专用测试库 public 只能用于只读 Flyway validate、结构/数据/Flyway 历史基线采集和前后比较。测试结束只能精确删除自己创建的随机 Schema，并必须证明 public 基线未变化。执行器环境变量只提供专用测试数据库连接，不得隐式决定迁移 Schema。

## 可验证 PIT 市场原始事实 V2 设计

59. **当前 AKShare-Tencent 行情链不具备已验证的 Provider revision**：现有 Controller → Java 行情服务 → Python AKShare/Tencent → `List<Bar>` → `MarketDataPersistenceService` → V9 链路只传递 symbol、来源名、交易日与行情数值；普通持久化入口固定使用 `sourceRevision=null`，`LOCAL_DATASET_V1-<UUID>` 只表示本地捕获数据集。`000001` 两次受控响应的行数、内容、原始 body 与 Tencent `version=18` 虽一致，但没有正式字段语义、历史版本、修订关系或发布时间证据，因此当前资格冻结为 `PROVIDER_REVISION_UNVERIFIED`。`version=18`、HTTP Date、UUID、batch/dataset/observation version、内容 Hash、抓取时间、数据库 ID 和请求序号均不得冒充 `sourceRevision`。
60. **V2 来源采用资格隔离而非跨来源覆盖**：Tushare 是 raw daily、独立 `adj_factor` 与 `trade_cal` 的优先技术候选，但只有本地持久化、历史回放、回测、派生计算和内部 Agent 使用权获得书面确认，且字段、单位、时间和修订样例通过验收后才可批准 Adapter。AKShare-Tencent 只保留研究级 current projection 和非权威交叉校验角色。Wind 等企业来源必须通过 revision/snapshot、旧版本、逐记录发布时间、因子和公司行动修订、交易日历修订、许可、价格与 SLA 的书面问询和样例门禁；iFinD/Choice 在证据取得前均为 `UNKNOWN`。每条事实只保留唯一 source lineage，不用跨来源多数表决改写原始事实。
61. **PIT_MARKET_FACTS_V2 把四类市场事实及来源身份独立版本化**：候选契约固定为 `PIT_MARKET_FACTS_V2`、`PIT_MARKET_FACTS_CANONICAL_V2`、`RAW_DAILY_BAR_OBSERVATION_V2`、`ADJUSTMENT_FACTOR_OBSERVATION_V1`、`TRADING_CALENDAR_OBSERVATION_V1`、`CORPORATE_ACTION_OBSERVATION_V1` 和 `QFQ_AS_OF_ENGINE_V1`。未复权日线、复权因子、交易日历与公司行动必须作为四类独立 append-only 观察保存，并分别携带证券、证券/因子、交易所日历和证券/事件来源身份；批次请求身份不得替代事实身份。同一 Provider 的交易所日历可供该交易所多只证券复用，但交易所、证券或 Provider 身份不得混用。幂等只比较覆盖事实业务值、字段资格、revision/assurance/usage、许可和全部合格 Provider metadata 的 semantic content hash；完全相同语义连续捕获不追加，任一语义变化和 A→B→A 追加版本。复权因子不得嵌入 raw bar，公司行动首版必须解释分红、送转、配股、拆并股、除权除息和修订 lineage。
62. **Provider PIT 与系统首次捕获 PIT 使用不同 knowledge-time**：`SYSTEM_KNOWLEDGE_PIT/SYSTEM_KNOWLEDGE_ONLY` 必须满足 `knownAt=firstObservedAt<=recordedAt`，只服务真实首次捕获之后的决策，并明确不证明供应商发布时间、历史完整性或首次捕获前可得性。`PROVIDER_PIT_VERIFIED/PROVIDER_VERIFIED` 只适用于 revision、逐版本发布时间、修订关系和许可已有正式证据的来源；必须有 providerRevision 与 providerPublishedAt，并满足 `knownAt=providerPublishedAt<=firstObservedAt<=recordedAt`，providerUpdatedAt 如存在必须位于 providerPublishedAt 与 firstObservedAt 之间。因此合格 Provider 版本允许在首次本地接收之前、但不早于其已验证发布时间的 cutoff 被 as-of 选择。`PROVIDER_UNVERIFIED`、`PROVIDER_UNAVAILABLE` 和 `SYSTEM_KNOWLEDGE_ONLY` 不得把候选 dataset/revision/snapshot/published/update 值写入合格 Provider 字段或可靠 Hash。
63. **QFQ V2 必须由 cutoff 前可见的 raw 与同日精确 factor 确定性重建**：`QFQ_AS_OF_ENGINE_V1` 的因子覆盖模式固定为 `DAILY_EXACT`。请求级日期使用 `requestEffectiveTradeDate/anchorTradeDate`，不得与因子字段 `factorEffectiveTradeDate` 混用；输入必须显式冻结 sourceCode 以及 raw、factor、calendar、corporate-action 四类来源身份。每根 raw bar 日期 `t` 只允许在同 Provider、正确证券/因子身份、同 `factorType` 且 `factorEffectiveTradeDate==t` 的精确自然键上选择 `knownAt<=knowledgeCutoff` 的资格优先、时间稳定 append-only 版本，锚点因子必须精确满足 `factorEffectiveTradeDate==anchorTradeDate`。V1 禁止 factor forward-fill、最近值替代、当前最新值补历史、从 QFQ 反推 factor、跨 Provider 拼接或对剩余窗口重新归一化；任一精确因子缺失或其 `knownAt` 晚于 cutoff 时返回 `PIT_FACTOR_UNAVAILABLE`。非 Provider verified 的 factor 修订只能由同 symbol/source、正确 action identity、相同 effectiveTradeDate 且不晚于当前因子可见时间的公司行动解释；无关日期或身份的 action 不得解除 `PIT_CORPORATE_ACTION_LINEAGE_UNAVAILABLE`。价格公式保持 `rawPrice(t,cutoff) * factor(t,cutoff) / factor(anchorTradeDate,cutoff)`；权威计算使用十进制 `BigDecimal`，除法 scale 16 `HALF_UP`，最终 OHLC scale 4 `HALF_UP`，volume、amount、turnover 不复权。Java 是 as-of 选择、QFQ、lineage 和 Hash 的唯一生产权威，Python 只验证固定结果向量和 Hash，不重算 QFQ。未来稀疏 effective-from step 因子必须建立独立 Provider 覆盖契约、证明生效区间并使用独立 engine 版本，不能由 V1 隐式推断。
64. **V2 设计和实现不改变 2F V1，也不授予真实 Provider 资格**：V9、`1.4.0-stage-2f-strategy-backtest-v1`、`AGENT_CONTEXT_2F_V1/BACKTEST_CONTEXT_V1`、旧 contextHash、缓存键和结果完全不变。3A-R3B-0 已使用独立 ruleVersion、contextProfile 和 Context Schema 实现并合入 Provider 中立的 TEST/DEMO 固定夹具、Mock Provider、四类事实、QFQ、2F V2 离线链路及保持禁用的 iFinD Adapter 骨架；V13 代码已经进入集成分支，但正常业务库尚未执行 V13。该实现不授予真实 Provider 资格，也没有发起 iFinD 调用。真实 Provider 字段映射、调用、Provider-backed 事实和资格声明仍必须通过独立来源审计、许可审查、实施验收和后续升级门禁。3A-R3B-0 完成不等于 Provider 获批、Day 002 获准、完整 3A 完成或 3B 开始。

## iFinD 试用里程碑启动治理

65. **iFinD 试用只由里程碑触发，不由日期触发**：iFinD 是由用户控制的有限 15 天试用窗口，启动不得绑定 `2026-08-31`、2026 年 8 月 31 日或任何其他固定日期。日历日期只能作为非权威临时估算，不能成为 `CURRENT_STATE.md`、`ROADMAP.md`、任务书、自动化配置或验收结论中的启动依据，也不得因预计日期临近而降低数据库、DTO、PIT、QFQ、Mock 闭环、证据采集或安全门槛。
66. **试用启动状态只有 PASS 或 BLOCKED，并受免费验证与付费决策前置约束**：正式标志只能是 iFinD 试用启动门的 `PASS` 或 `IFIND_TRIAL_ACTIVATION_GATE=BLOCKED`。PASS 必须先证明 3A-R3B-F0、F0.5、F1、F2A、F2B 与 F3 已完成相应验收，且付费升级决定已经另行判定为 `PROCEED`，再同时证明 Provider 中立接口与 DTO、四类 PIT 事实及隔离 PostgreSQL、append-only/幂等/A→B→A/cutoff、`DAILY_EXACT` QFQ 18 个黄金场景、2F V2 Mock、六智能体 Mock、EXPLICIT Mock Shadow、保持禁用的 iFinD Adapter 骨架和错误处理、函数/字段/证券/日期/调用预算清单、响应证据采集与脱敏工具、无待决重大数据库或 DTO 重构，以及用户可安排连续 15 天联调共 12 项既有条件全部通过，并由用户亲自批准申请和激活；任一项缺失即为 BLOCKED。ChatGPT 在每个相关阶段基于实际 Git 提交和运行证据同步检查门禁，只有正式 PASS 后才建议用户开通，不得用预计日期或接近完成代替 PASS。
67. **iFinD 的申请、激活、调用和凭据由用户控制**：Codex 不得申请、激活、续期或自动调用 iFinD；试用前真实 iFinD 调用数必须为 0。只有用户看到正式 PASS 并亲自批准申请与激活后，才可开始连续 15 天集中接入与取证。用户名、密码、token、session、Cookie、认证头和完整敏感 URL 不得写入 Git、文档、日志、截图或测试夹具；凭据只能通过批准的安全运行时机制注入。试用调用必须受批准的函数清单与预算约束，禁止 scheduler、全市场遍历、无界自动重试和自动交易。
68. **有限试用不成为离线能力或产品验证的单点依赖**：3A-R3B 必须先完成 Provider 中立离线闭环和免费 Provider 资格审计；产品形态可按 `RESEARCH_PREVIEW_FIRST` 在资格隔离的 F2A 中先行验证，Provider-backed 产品与前向效果仍必须依次完成 F1、F2B 和 F3。只有系统显示可重复使用价值、数据成为量化主要瓶颈且付费升级决定为 `PROCEED` 时，才进入 iFinD 启动门、15 天真实取证和 Provider 资格判定。试用结束、额度耗尽或账号停用不得破坏 `SYSTEM_KNOWLEDGE_PIT`、Mock Provider、脱敏固定夹具、免费版本和离线回归。最终资格只能依据真实证据与书面许可判定为 `PROVIDER_PIT_VERIFIED`、`PROVIDER_REVISION_UNVERIFIED`、`PROVIDER_REVISION_UNAVAILABLE`、`SYSTEM_KNOWLEDGE_PIT`，或许可不足/不批准接入；资格结论经 ChatGPT 对实际 Git 和运行证据验收并由用户批准前，不得恢复 Day 002，也不表示完整 3A 或 3B 已开始。

## Provider 中立 PIT 市场事实 V2 离线实现

69. **V2 实现使用独立版本和 V13，不改写任何旧契约**：3A-R3B-0 固定使用 `1.4.0-stage-3ar3b0-agent-team-pit-v2`、`AGENT_CONTEXT_3AR3B0_V2`、`BACKTEST_CONTEXT_V2`、`BACKTEST_CANONICAL_V2`、`PIT_MARKET_FACTS_V2`、`PIT_MARKET_FACTS_CANONICAL_V2` 与 `MARKET_FACT_PROVIDER_CONTRACT_V1`。V13 只前向新增 Provider 中立的 batch、observation、raw daily、adjustment factor、trading calendar 和 corporate action 事实，并扩展 TEST/DEMO Shadow 允许规则；V1 至 V12、V9 当前 QFQ、2F V1、旧 profile/contextHash/cache/result 均保持不变。V13 不回填 `daily_bars`、V9 或正常业务数据。3A-R3B-0 已通过 ChatGPT 对实际 Git 提交的最终复验并经用户批准合入集成分支；正常业务库仍未执行 V13，因此代码集成状态不得被描述为正常业务库已经部署 V13 或具备真实 Provider 数据。
70. **Provider 返回值与 Java 本地事实权威严格分层**：Provider 公共 DTO 可以表达四类上游事实、每类事实自身的稳定来源身份、capability、revision/snapshot/published/update time、许可、覆盖、单位、精度、完整性、错误和限流，但不得生成 `firstObservedAt`、`knownAt`、`recordedAt`、本地 datasetVersion、observationVersion 或 canonical Hash。后者只能由 Java 权威捕获、生成或验证。本地 UUID、时间、Hash 和版本不得提升为 Provider revision；未验证候选版本值不得进入合格 Provider metadata。raw 的 OHLC 必填，volume、amount、turnoverRate 可空且必须分别携带 `PRESENT_VERIFIED/PRESENT_UNVERIFIED/MISSING`、冻结单位和语义代码；PRESENT 必须有值、MISSING 必须为 null，明确 0 不等于缺失。QFQ 可保留这些非价格字段，但 2F V2 缺少合格 volume 时固定返回 `PIT_REQUIRED_MARKET_FIELD_UNAVAILABLE`。
71. **V2 事实链和 QFQ 必须完整或安全不可用**：四类事实均为 append-only；完整 semantic content hash 相同才幂等，业务值相同但资格、许可、providerSnapshotId、providerPublishedAt 或 providerUpdatedAt 变化仍必须追加，资格 A→B→A 保留第三个物理版本，并发捕获只允许一个合法链尾。as-of 查询必须显式接收 sourceCode、事实来源身份和 knowledgeCutoff，并对 calendar、raw、factor、corporate action 统一执行“先选版本、后验许可”：先在 cutoff 可见的同来源版本中按 revision qualification 优先级、knownAt、chainSequence、id 选出唯一语义版本，避免较晚低资格版本遮蔽已验证版本；再检查 usage qualification、local persistence、historical replay、backtest 和 Agent use。选中版本任一必要许可为 false 时固定返回 `PIT_USAGE_NOT_ALLOWED`，禁止在选版前过滤、回退旧允许版本或伪装成数据缺失；允许→禁止→重新允许必须保留三个版本和三个确定 cutoff 区间。batch 的 `providerDatasetVersion` 也只有 `PROVIDER_VERIFIED` 可以携带，其他 revision qualification 必须为 null。`QFQ_AS_OF_ENGINE_V1` 继续执行 `DAILY_EXACT` 和公司行动精确关联；任何 calendar/raw/factor/action lineage 缺失均返回稳定不可用结果，不得输出部分窗口。18 个黄金场景必须是含固定事实输入、时间、资格、预期输出、lineage 和 Hash 的可执行向量，由 Java 参数化运行引擎逐项断言，Python 只验证提交结果而不得重算第二套 QFQ。
72. **Mock 和 iFinD 骨架都不能取得真实来源资格**：3A-R3B-0 Mock Provider 仅允许 TEST/DEMO 合成固定夹具、`formalEligible=false` 和显式测试 profile，禁止网络并不得用于 Day 002。iFinD Adapter 默认 disabled，不安装 SDK、不创建真实客户端、不读取凭据；即使误配置 enabled，也必须在网络动作前以 `IFIND_TRIAL_GATE_NOT_PASSED` 失败。真实函数、字段、权限、额度、许可、revision 和 snapshot 均保持 `UNVERIFIED`，真实调用数保持 0。
73. **2F V2、六智能体和 Shadow 只对精确 V2 规则显式启用**：2F V2 由 Java 从四类事实生成 QFQ、回测、lineage 和 Hash，继续复用既有 SMA20、`BACKTEST_ENGINE_V1` 和七项参数；Python 只解释冻结结果，不计算 QFQ、回测或访问 Provider/数据库。六 run 数量、顺序、POSITION_RISK 唯一正式 veto、2I 总控权重/优先级/终态和旧版本行为均不变。Shadow 生产默认规则和 scheduler 默认关闭状态不变；V2 仅能在 TEST/DEMO、EXPLICIT、随机隔离 Schema 中经既有 Java 任务系统执行。

## 免费优先 Provider 验证与付费升级

74. **免费数据先验证产品和效果，付费 Provider 后置升级**：系统在真实产品形态和效果尚未证明前，不把 iFinD 或其他高成本数据作为开发前置依赖。路线固定为免费数据完成产品与效果验证，证明系统具有可重复使用价值，判断数据是否已成为主要瓶颈，再决定是否启动付费试用；付费数据必须在同股票、同日期、同策略、同参数范围内与免费数据 A/B 对照，只有可量化提升足以覆盖成本时才考虑长期购买。iFinD 只是系统证明有效后的专业化升级候选。免费路线不得冒充专业 Provider 资格，也不得降低 PIT、许可、lineage、QFQ、knowledge-time 或用途门禁。

75. **免费 Provider 候选必须逐来源审计，开源客户端不等于数据许可**：3A-R3B-F0 只把 BaoStock 视为免费主 Provider 技术候选，把 AKShare/Tencent 视为研究级当前投影、辅助和交叉校验候选，把巨潮资讯、上交所、深交所公开信息视为公告、公司行动、交易日历和规则的官方证据候选；任何候选只有经过 raw daily、独立 factor、`DAILY_EXACT`、日历、公司行动、四类来源身份、单位/空值/时效、静默修正、限制与失败、本地保存、历史回放、回测、Agent、商业化、revision/snapshot/published/update、旧版本和维护风险证据审计后才能进入 Adapter 决策。不得因客户端开源推断底层数据可商业使用，不得从 QFQ 价格反推因子，也不得跨 Provider 拼成伪造的同源 PIT lineage。

76. **免费数据必须区分研究历史事实与系统前向知识事实**：免费 Provider 的历史数据在没有 Provider PIT 证据时只能标记 `RESEARCH_HISTORICAL_UNVERIFIED`，可用于产品、演示、探索性历史回测、覆盖研究和交叉校验，但不得宣称 `PROVIDER_PIT_VERIFIED`、历史时点绝对可得、无前视、商业正式数据或真实历史修订版本。只有系统首次真实捕获后，满足 append-only、`firstObservedAt`、`knownAt`、cutoff 和许可门禁的事实才可形成 `SYSTEM_KNOWLEDGE_PIT`；它只证明系统从首次捕获以后在决策时点知道什么，不证明 Provider 在首次捕获前何时发布或修订。

77. **免费产品闭环与 Shadow 使用预先冻结的完整评估指标**：3A-R3B-F2A 必须让用户看见候选池、单股完整分析、六智能体、总控、数据质量、各专业结论、evidence/lineage/reasonCode、历史查询、结果对比和数据资格，并严格区分现有研究快照、不可用数据与显式 TEST/DEMO；页面完成度不得被当作选股有效。F2B 才能使用 F1 合格 Provider-backed 事实和 `SYSTEM_KNOWLEDGE_PIT` 形成真实产品闭环。F3 只接受 F1/F2B 输入，并在 Shadow 前冻结 `FREE_VALIDATION_METRICS_V1`，至少评估 5/10/20 个交易日、相对基准命中和平均/中位超额、MFE/MAE、最大回撤、盈亏比、换手和成本、市场环境、confidence 区分度、阻断率、Agent 边际贡献、重放/Hash 与随机/固定基准；冻结后不得移动阈值、周期、基准或样本，不得只报告上涨或盈利样本，也不得删除失败、阻断或无信号样本。F2A 不得进入正式 F3 样本，完整 F3 仍至少需要 20 个有效观察日和 200 个 Shadow item。

78. **产品预览门、免费验证门与付费升级决策彼此分层**：`FREE_PRODUCT_PREVIEW_GATE` 只允许 `PASS/BLOCKED`，当前为 `PASS`；该 PASS 只表示用户认可 F2A 产品形态和日常只读流程，不表示 Provider、PIT、策略、Shadow、iFinD 或交易获批。`FREE_PROVIDER_VALIDATION_GATE` 只允许 `PASS/BLOCKED`，当前为 `BLOCKED`；PASS 只表示至少一套免费路线在明确用途边界内稳定驱动 F2B 产品闭环和 `SYSTEM_KNOWLEDGE_PIT` 前向 Shadow，F2A 不计入该门禁，也不表示商业正式许可、Provider PIT、盈利、iFinD 或 Day 002 获批。`PAID_PROVIDER_UPGRADE_DECISION` 只允许 `PENDING/DEFER/PROCEED`，当前为 `PENDING`；只有产品获用户认可、免费数据稳定驱动闭环、Shadow 显示不只是大盘或随机波动的初步可重复价值、数据成为量化主要瓶颈、付费改善指标和同范围 A/B 已明确、无重大重构且用户愿意承担成本时才能 `PROCEED`。产品或规则问题更大、免费效果未证明、免费数据已足够、增量不能覆盖成本或用户暂不投入时可 `DEFER`，DEFER 不等于项目失败。只有 F0、F0.5、F1、F2A、F2B、F3 相应验收完成、决策为 `PROCEED`、既有 12 项 iFinD 准备条件全部满足、用户可安排连续 15 天且亲自批准申请和激活后，iFinD 试用启动门才能另行通过。

79. **F0 审计结果冻结为 PARTIAL，不自动打开免费验证门**：3A-R3B-F0 的逐来源、逐事实审计与最小受控探针结论为 `F0_AUDIT_RESULT=PARTIAL`。该状态只表示存在可用研究数据和部分事实能力，但仍缺书面许可、`DAILY_EXACT`、交易所级日历身份、稳定公司行动版本、Provider revision/snapshot/published/update time 和旧版本证据；它不等于 F1 获批，也不改变 `FREE_PROVIDER_VALIDATION_GATE=BLOCKED`、`PAID_PROVIDER_UPGRADE_DECISION=PENDING` 或 `IFIND_TRIAL_ACTIVATION_GATE=BLOCKED`。

80. **BaoStock 只取得部分技术证据，最终角色为 PENDING_WRITTEN_PERMISSION**：F0 固定 BaoStock 客户端版本为 `0.9.3`；已验证 PyPI wheel SHA-256 `acbd19403285bc4e254cee8297cf0e2646ae2276e5af7e549deed3988ab02293` 和 sdist SHA-256 `16699d82d05037a8c133577fcdeb9ac0d5a7f31edc2432c4e883004e0a95e3f7`。匿名单会话固定探针中，两只证券的 raw/QFQ 短日线各观察到 6 行，通用日历观察到 8 行，公司行动观察到 1 行，两个按证券因子查询各观察到 0 行；修复前 collector 未在迭代后复核 Provider 终态，原始响应又已按边界删除，因此本次 Live response completeness 统一为 `UNVERIFIED`，不得把观察行数写成完整响应。全市场单日因子函数因范围边界未调用。独立 factor 技术入口评为 `PARTIAL`，`DAILY_EXACT=UNVERIFIED`，通用日历也没有 exchange identity。客户端 BSD License 不能推导底层数据的本地保存、回放、回测、Agent、UI、备份或商业权利；在取得适用于具体数据用途的书面许可和正式数据字典前，BaoStock 不得成为 `FREE_PROVIDER_F1_CANDIDATE`、`APPROVED_ADAPTER` 或 `PROVIDER_PIT_VERIFIED`。

81. **AKShare 必须按实际上游拆分，不能形成共同 sourceCode**：仓库继续固定 `akshare==1.18.64`。`stock_zh_a_hist_tx` 的上游是 Tencent QQ Finance，`stock_zh_a_daily` 的上游是 Sina Finance，`stock_zh_a_hist` 的上游是 Eastmoney，`stock_zh_a_disclosure_report_cninfo` 的上游是 CNINFO；它们必须具有不同的 sourceCode/source identity 和独立许可，不得用客户端名 `AKSHARE` 合并 lineage。Tencent 仍为 `PROVIDER_REVISION_UNVERIFIED`，`version=18` 不得接入；Sina 代码虽暴露 qfq/hfq factor 模式，但 F0 没有 Live 验证、`DAILY_EXACT` 或许可证据；Eastmoney 当前函数没有独立 factor；AKShare/CNINFO 继续沿用 2G 的 RESEARCH、DATE_ONLY 和无修订关系保证。四条路线最终角色均为 `RESEARCH_AUXILIARY_ONLY`。

82. **CNINFO 和交易所公开页面只承担官方证据角色**：CNINFO 法定披露平台页面可核验公告身份和披露元数据，SSE schedule 可核验交易时段和休市安排；SZSE/SZSI 页面本轮受控读取超时。公开展示、免责声明或交易日历证据不能自动授予行情/公告批量下载、本地持久化、回测、内部 Agent 或商业用途，也不能替代机器可读版本、修订关系和正式数据服务许可。CNINFO、SSE、SZSE 与深圳证券信息有限公司当前角色统一为 `OFFICIAL_EVIDENCE_ONLY`；不得把多个官方证据页拼成一个行情 Provider。

83. **F0 审计工具默认断网且只保存安全摘要**：`free_provider_audit_f0.py` 与生产 FastAPI、Agent、V13 和数据库完全解耦；默认网络关闭，只有显式 `--live` 才允许固定两证券、固定短区间、最多 10 个 BaoStock 逻辑调用。它拒绝全市场函数，不读取 `.env` 或业务配置，不访问数据库或 iFinD，递归脱敏敏感字段，只保留字段名、统计、日期范围、状态和 Hash；公开函数证据只保存参数名、参数 kind 和是否存在默认值，不保存默认值。具体价格、成交量、成交额、因子和公司行动数值只在操作系统临时目录短暂存在，计算 Hash 后删除。F0 实际数据调用为 8 个逻辑调用，另有 2 个匿名登录/退出公开操作；socket 级协议请求/frame 数未观测，必须固定为 `UNVERIFIED`，不得由函数调用数推断。Provider HTTP 请求 0，重试 0，原始响应残留 0。原 V1 安全摘要 Hash 由修复前 collector 生成，只证明该摘要内容，不证明 Provider 终态响应完整性，也不能成为 Provider revision、历史可见性或许可证据。修复后 collector 必须在迭代结束后重读 `error_code/error_msg`，有行且终态错误为 `PARTIAL`，无行且终态错误为 `ERROR`，仅终态成功才可标记 `SUCCESS/EMPTY`。

## 免费版实施范围与双轨路线

84. **F0 已验收合入但 PARTIAL 不等于失败或 F1 获批**：3A-R3B-F0 最终提交 `059eacffaf7e4a9f383be205d453c5168279932a` 已通过 ChatGPT 对实际 Git 提交的最终复验，并经用户批准纯 fast-forward 合入。`F0_AUDIT_RESULT=PARTIAL` 表示 BaoStock 具有部分技术能力但仍为 `PENDING_WRITTEN_PERMISSION`、`DAILY_EXACT=UNVERIFIED`，AKShare 各实际上游只具有 `RESEARCH_AUXILIARY_ONLY` 角色，CNINFO/SSE/SZSE/SZSI 只具有 `OFFICIAL_EVIDENCE_ONLY` 角色；当前没有免费来源能单独承担完整 V13/QFQ 同源 lineage。该结论既不是审计失败，也不改变 F1、Provider 或 PIT 门禁。
85. **免费实施路径固定为研究预览先行**：正式状态 `FREE_IMPLEMENTATION_PATH` 固定为 `RESEARCH_PREVIEW_FIRST`。轨道 A 为 F0.5 → F2A → 用户产品形态验收；轨道 B 为 F0 → 书面许可或替代 Provider 证据 → F1 → F2B → F3。两轨可以等待或并行推进，但资格必须隔离：轨道 A 的页面、交互和报告可见性不能证明轨道 B 的许可、source identity、`DAILY_EXACT`、knowledge-time、Provider PIT 或前向 Shadow 已完成。
86. **F2A 只能使用现有研究快照与显式 TEST/DEMO**：F2A 真实本地输入必须只读并标记为 `EXISTING_RESEARCH_SNAPSHOT/RESEARCH_HISTORICAL_UNVERIFIED`；3A-R3B-0 固定 Mock 必须标记为 `TEST_DEMO_EXPLICIT`，不得与真实研究数据混写。既有 Agent、总控和历史结果只能按原规则读取、解释和展示，不得改写权威结果。可靠回测不可用时必须展示结构化原因；允许用显式 TEST/DEMO 展示页面结构，但不得把 Mock 收益写成真实历史收益。F2A 不新增外部调用、不写 V13、不迁移正常业务库、不创建 Day 002、不运行正式 Shadow，也不宣称准确率、推荐有效或盈利。
87. **产品预览门不替代 Provider 验证或效果验证**：`FREE_PRODUCT_PREVIEW_GATE` 只允许 `PASS/BLOCKED`，当前为 `PASS`。用户已看到候选池和单股完整分析，理解六智能体与总控，查看 evidence、lineage、reasonCode 和数据资格，并确认真实研究数据、不可用数据和 TEST/DEMO 的展示边界；用户已经明确认可产品总体形态和日常只读流程。该 PASS 不表示 `FREE_PROVIDER_VALIDATION_GATE`、Provider PIT、策略、Shadow、iFinD、Day 002、生产、商业化或交易获批。
88. **F1、F2B 与 F3 继续受来源资格和前向事实约束**：BaoStock 在书面个人研究调用、本地持久化、历史回放/回测、内部 Agent、数据字典、单位与稳定身份解决前继续为 `PENDING_WRITTEN_PERMISSION`；完整 QFQ 还必须解决独立 factor、`DAILY_EXACT`、交易所日历身份、公司行动身份和单 Provider lineage。F2B 只能在 F1 验收完成并可合法积累 `SYSTEM_KNOWLEDGE_PIT` 后开始；F3 只能使用 F1/F2B 合格输入，并继续要求 `FREE_VALIDATION_METRICS_V1`、不少于 20 个有效观察日和 200 个 Shadow item。F2A 不能进入正式 F3 效果样本。
89. **新免费来源调查必须有界且单独授权**：可以继续审计免费或低成本来源，但必须作为独立阶段由用户授权，优先验证独立 adjustment factor、`DAILY_EXACT`、同源 raw/factor/calendar/action、本地持久化、回测、Agent 使用、revision/published time 和调用稳定性。不得为了继续开发而无边界搜索免费接口；F2A 产品形态现已获得用户认可，但该结果本身仍不授权启动新 Provider 调查或 F1。

## 免费研究预览产品

90. **F0.5 已合入，F2A 技术实现不自动打开产品预览门**：3A-R3B-F0.5 最终提交 `08943b4f6af03c75aa4df2a4ecf2494bede4e57b` 已通过 ChatGPT 实际 Git 验收并经用户批准纯 fast-forward 合入。用户随后单独授权 F2A；技术实现、静态安全检查、类型检查、生产构建、ChatGPT Git 验收或 merge 都不能单独把产品预览门改为 PASS。该决策在 F2A、R1、R1A 和 R1B 期间均被遵守：各轮技术完成后门禁仍保持当时的 `BLOCKED`，直到用户实际查看最终页面并明确认可，才由独立治理提交变更。
91. **F2A 只读复用既有 GET API，不增加服务端能力**：研究预览使用独立 `/research-preview` 路由，只读取既有扫描历史/结果与 Agent task/run/evidence/decision/veto GET 接口。专用 API 和页面禁止 POST、PUT、PATCH、DELETE、原生 fetch 绕过、任务创建、扫描/回测启动、行情刷新、公告摄取、Shadow、Portfolio 写路径或 Provider 调用。F2A 不新增 Java/Python/SQL/Flyway，不访问正常业务库，不执行 V13；API 不可用是正常结构化状态，不构成新增后端接口的理由。
92. **本地研究快照与显式 Demo 必须在状态、身份和对比中隔离**：本地模式固定为 `EXISTING_RESEARCH_SNAPSHOT/RESEARCH_HISTORICAL_UNVERIFIED/READ_ONLY`，只展示已持久化权威结果；Demo 固定为 `TEST_DEMO_EXPLICIT/SYNTHETIC/NOT_REAL_MARKET_RESULT`，使用非真实 `DEMOxx` 证券、负数本地占位 taskId、精确六 run、唯一 evidenceId 和非真实 Hash 占位，不包含真实市场数值或 Provider 身份。API 失败不得静默切换 Demo；两模式候选、任务、图表、对比和报告不得混合，Demo 不进入任何网络、持久化、Agent、Shadow 或交易接口。
93. **F2A 是展示层，不复制或推断权威业务规则**：六 run 顺序和 POSITION_RISK 唯一正式 veto 保持不变；缺失 score/confidence 显示“暂无”且不绘制为 0。总控、回测、风险和 evidence 只展示现有值，不重算评分或综合结论。reasonCode 只来自 finding code、结构化 Agent error、正式 vetoCode、task/run error 或固定 UI code，不从 summary 自然语言猜测。综合报告必须标记 `UI_PRESENTATION_ONLY`，只确定性组织已有元数据、summary、finding、reason 和资格，不调用 LLM、不预测、不生成买卖/仓位建议，也不宣称准确率或收益。
94. **F2A 已合入但首次视觉验收保持 BLOCKED**：F2A 最终提交 `f5137e2422a48e70d5c706cb146fb034a2b96f65` 已通过 ChatGPT 对实际 Git 提交的验收，经用户批准纯 fast-forward 合入。用户于 `2026-07-29` 根据完整页面截图完成首次视觉验收，确认双模式隔离、候选池、固定六智能体、总控、证据、历史对比和报告的技术闭环可见；但页面纵向过长、默认技术字段过多、字体和层级不足、Demo 标签重复且报告偏日志化，因此视觉验收为 `BLOCKED`，不能据此打开产品预览门。
95. **F2A-R1 只收敛展示层且固定五分区与研究动作映射**：研究预览固定为“研究总览、六智能体、证据与审计、历史对比、综合报告”五个主分区，默认进入研究总览。首屏研究动作只允许按现有总控 code 确定性映射为“因正式风险否决停止研究、等待数据质量修复、暂不形成研究结论、仅作研究记录、继续观察、进入人工研究复核”；该映射是中文展示，不新增或改写权威决策。技术审计、Agent 完整字段、证据详情、任务对比和原始报告默认折叠，但必须可完整追溯。
96. **F2A-R1 不改变资格、只读或权威规则边界**：R1 继续 GET-only，保持 `EXISTING_RESEARCH_SNAPSHOT/RESEARCH_HISTORICAL_UNVERIFIED` 与 `TEST_DEMO_EXPLICIT/SYNTHETIC/NOT_REAL_MARKET_RESULT` 隔离，不修改 Demo 事实、Agent 规则、总控、数据资格、六 run 顺序或 POSITION_RISK 唯一正式 veto，不新增 Provider、数据库、Agent、Shadow 或交易写路径。R1 技术实现、测试、Git 验收或 merge 当时均未打开产品门；用户随后通过实际查看 R1A/R1B 最终页面并明确认可，才由独立治理提交记录 PASS。
97. **F2A-R1A 分离数据门禁、证据完整性和风险颜色语义，R1B 最终采用垂直流**：R1 最终提交 `e2b9457e3594676875167a703ae09ebc75aaaaf6` 已通过 ChatGPT 实际 Git 技术复验并经用户批准纯 fast-forward 合入；用户于 `2026-07-29` 完成第二次视觉复验，主体信息架构通过但该轮产品门仍为 `BLOCKED`。R1A 废止含混的“数据可靠性”：DATA_QUALITY 的 status/gate 只映射为“数据质量门禁”，总控阻断/不足和 STRATEGY_BACKTEST 结构状态只映射为“研究证据完整性”，两者都不是新评分或可靠性认证；DEMO01 固定表达“门禁通过、证据不足”。综合报告风险 tone 只允许 `INFO→info`、`WARN→warning`、`HIGH/CRITICAL→danger`、`FORMAL_VETO→formal-veto`、未知→`neutral`，普通 INFO 不得显示为严重红色，正式 veto 仍只能来自 POSITION_RISK。R1A 的双列 Grid 是该轮历史实现；R1B 最终提交 `4917bbabc8262106abb47e6cb90cf7ab96e76d7d` 将头部替换为单列正常文档流，且不修改 Demo fixture、GET-only、Agent/总控、六 run、资格或 Provider/数据库边界。
98. **用户最终产品形态验收后，FREE_PRODUCT_PREVIEW_GATE 变更为 PASS**：用户基于最终产品代码基线 `4917bbabc8262106abb47e6cb90cf7ab96e76d7d` 完成 DEMO01 与 DEMO02 最终视觉复验，并于 `2026-07-29 16:44 +08:00` 明确回复：“认可当前产品形态，批准创建独立治理提交，将FREE_PRODUCT_PREVIEW_GATE改为PASS。”因此当前正式状态为 `FREE_PRODUCT_PREVIEW_GATE=PASS`。该 PASS 只证明用户已经实际查看并认可免费研究预览的信息架构、产品形态、日常只读研究流程，以及六智能体、总控、证据、历史和报告的展示方式，Track A 的产品形态验证完成；它不证明免费 Provider、真实数据、Provider PIT、V13 正常业务库、策略效果、回测收益或 Shadow，不授权股票推荐、投资建议、券商账户、真实交易、自动交易、iFinD 或付费采购，也不自动启动 F1、F2B、F3 或其他后续阶段。产品预览门与当前仍为 `BLOCKED` 的免费 Provider 验证门保持独立，付费升级决定继续为 `PENDING`，iFinD 试用启动门继续为 `BLOCKED`。

## Track B Provider 路线与 F1 准入

99. **Track B0 只比较 BaoStock、Tushare Pro 与同花顺 iFinD，并以官方资料为决策证据**：Track B 的入口调查必须保持有界；能力、许可、价格与接口结论只使用 Provider 官方网站、官方 API 文档、官方服务协议、官方价格/申请页和官方联系方式。第三方文章、搜索摘要、论坛、代理报价或未核验客服截图不得支撑正式结论。调查完成不等于 Adapter 获批，也不允许真实 Provider 调用。

100. **Track B 主要路线固定为 Tushare Pro 低成本正式数据，备用路线固定为 iFinD**：正式选择为 `TRACK_B_PRIMARY_ROUTE=LOW_COST_PROVIDER_FIRST`，其具体候选是 Tushare Pro；备用选择为 `TRACK_B_FALLBACK_ROUTE=IFIND`。Track B0/F1A 时点的加权评分为 BaoStock、Tushare Pro、iFinD 分别 `1.98/3.10/2.10`，其中 Tushare 法律项当时只有 `2.5`；这是收到 TS-WP-002 前的历史评分。F1D 后的当前重算见决策 131。排名只用于比较，硬性许可、同源事实、稳定身份、前向 PIT 与成本门禁优先。BaoStock 只保留免费研究辅助与书面许可候选角色，不作为当前备用路线。

101. **Tushare Pro 当前只能取得 V13、PIT 与证券身份的部分资格**：官方文档已证明同平台提供未复权 `daily`、独立 `adj_factor`、SSE/SZSE `trade_cal`、`dividend`、`stock_basic`、停复牌、ST、指数行情/成分以及 HTTP/Python 接入；`pro_bar` 的 QFQ 会依赖查询结束日动态锚定，因此不得直接作为历史不可变事实。当前 `V13_LINEAGE_PARTIAL`、`PIT_PARTIAL`，稳定证券 ID 也只能为 `PARTIAL`：`ts_code` 带交易所后缀并贯穿核心接口，`stock_basic` 存在上市、退市和交易所字段，但永久 instrument identity、换码、迁板、重新上市及历史映射没有官方保证或样例。Provider 也没有公开历史 revision。B0/B1 时点本地保存、回测和 Agent 用途仍待书面许可；F1A 后续只取得“可作为量化数据来源”的官方书面证据，没有取得三项具体 Provider 书面确认。用户的有界个人实现授权允许缩小 Adapter 和隔离联调继续，但不改变 V13、PIT 或稳定身份的部分状态。服务终止后留存仍未验证，原始数据再分发未获授权，且不得声明 `PROVIDER_PIT_READY`。

102. **F1A 时点仍需逐项用途澄清（历史决策）**：官方服务协议与缓存/回测/AI 页面不能自行授予用途；`2026-07-30` 的 TS-WP-001 当时只确认“可以用来当量化数据来源”，所以 F1A 时点本地保存、回测、Agent 和到期留存仍为 `UNVERIFIED`。`2026-07-31` 收到 TS-WP-002 后的当前许可状态由决策 127—130 取代；原始数据再分发继续为 `NOT_GRANTED`。

103. **Track B0 时点的 F1 准入判断为三类多重阻断**：Track B0 固定 `F1_ENTRY_READINESS=BLOCKED_MULTIPLE`。当时阻断一是 Tushare Pro 本地持久化、回测、内部 Agent、长期保存和本地展示的书面许可；阻断二是公司行动完整性、`DAILY_EXACT` 实样、证券身份生命周期和修订/发布时间等技术证据；阻断三是官方 2000 积分个人价格虽已公开但用户当时尚未批准成本。B1 后续探针与成本复核可以缩小阻断组成，但 Track B0 合入本身始终不授权 F1。

104. **iFinD 只作为专业备用路线，当前不启动 15 天试用**：官方资料已证明其提供多语言 SDK/HTTP、历史数据、复权因子、交易日历、公告与调用额度等专业能力，但个人购买资格、API 授权范围、本地保存/回测/Agent 权利、试用结束后的数据处理、正式价格、V13 四类事实字段和 Provider revision/历史版本语义均需官方报价、书面确认和未来受控探针。Track A 通过只证明产品形态，不证明策略价值、数据瓶颈或付费升级收益；因此 `PAID_PROVIDER_UPGRADE_DECISION=PENDING` 与 `IFIND_TRIAL_ACTIVATION_GATE=BLOCKED` 保持不变，Codex 不得申请、激活或调用 iFinD。

105. **Track B 技术探针固定为最小、隔离且不持久化业务库**：低成本 Provider 或 iFinD 只有在法律、账户和用户专项授权前置满足后，才允许针对两只非敏感测试股票、两个历史交易日，在最多 10 次业务请求内验证 raw daily、adjustment factor、trading calendar、corporate action、security identity 和 published/effective/revision 字段。完整合同还要求 Provider 书面允许最小自动 API 探针，并书面确认临时响应、Hash、摘要和夹具的保存/删除范围。禁止全市场抓取、scheduler、Day 002、正常业务库写入和自动重试；原始响应只能进入临时隔离环境并在结束后按边界清理。B1 已执行其中的 Tushare 技术权限子集，但执行前没有取得上述两项 Provider 书面答复，因此完整证据探针状态为 `PARTIAL_NOT_COMPLETE`、完整法律前置为 `NOT_MET`；这不判定本次调用合法或违法，也不要求重新执行。iFinD 探针仍未执行。

106. **Track B0 不改变四项正式门禁或 Track A 结果**：当前继续保持 `FREE_PRODUCT_PREVIEW_GATE=PASS`、`FREE_PROVIDER_VALIDATION_GATE=BLOCKED`、`PAID_PROVIDER_UPGRADE_DECISION=PENDING`、`IFIND_TRIAL_ACTIVATION_GATE=BLOCKED`。主要路线选择不等于免费 Provider 验证通过，不等于付费升级决定为 PROCEED，也不批准 V13 正常业务库迁移、F2B、F3、Day 002、scheduler、3A-R3B-1、3B、投资建议或真实/自动交易。

107. **Tushare 2000 积分十项受控权限探针只升级最小技术样例**：用户已开通 Tushare 2000 积分权限；`2026-07-30` 使用 Python `3.11.9`、tushare `1.4.29`、pandas `3.0.5`，对 `600000.SH/000001.SZ` 和固定两交易日执行精确 10 次无重试业务请求，精确执行时刻统一为 `PROBE_EXECUTION_TIME=UNKNOWN`。两次 `stock_basic`、两次 SSE/SZSE `trade_cal`、两次 `daily`、两次 `adj_factor` 和两次 `dividend` 全部 `PASS`，权限错误与网络错误均为 0，综合为 `TUSHARE_2000_PERMISSION_PROBE=PASS`。完整响应、CSV、Token 和支付隐私没有留存，临时环境残留为 0。执行前未取得 Provider 对自动探针和响应留存/删除范围的两项书面答复，故 `TRACK_B_FULL_EVIDENCE_PROBE_STATUS=PARTIAL_NOT_COMPLETE`、`TRACK_B_FULL_PROBE_LEGAL_PREREQUISITES=NOT_MET`，两项书面许可均为 `UNVERIFIED`。该 PASS 只证明当前技术接口权限，不证明用途许可、完整探针成功或长期服务能力。

108. **B1 验证 raw/factor/calendar/普通身份/dividend 和最小 DAILY_EXACT，但不升级 V13、PIT 或永久身份**：两证券、两日 raw daily 和同日 adjustment factor、SSE/SZSE 日历、stock_basic 普通身份字段及 dividend 公开字段取得真实最小样例，因此这些最小样例为 `VERIFIED`。`dividend` 仍未证明配股、拆并股、更正/撤回、稳定事件 ID 或 factor/action 解释关系；没有 revision/snapshot/published/update/旧版本证据，也没有换码、迁板、重新上市或历史身份映射。因此当前继续为 `V13_LINEAGE_PARTIAL`、`PIT_PARTIAL`、稳定证券 ID `PARTIAL`；B1 技术样例本身不得写正常业务库或声明 `PROVIDER_PIT_READY`。后续 F1A 仅可在用户批准的有限个人范围内，经专用类型化授权入口写随机隔离测试 Schema。

109. **Tushare 最低成本批准阻断解除，但专业付费升级决定不变**：用户已实际开通 2000 积分，受控探针证明权限生效；不记录订单号、手机号、支付渠道或其他隐私。因此 `BLOCKED_COST_APPROVAL` 不再是 Tushare F1 当前阻断。`PAID_PROVIDER_UPGRADE_DECISION=PENDING` 仍控制后续专业付费升级和 iFinD，不因本次低成本权限自动变为 `PROCEED`。

110. **B1 至 F1C 时点的完整 F1 保持两类多重阻断（历史决策）**：B1 合入时 `F1_ENTRY_READINESS=BLOCKED_MULTIPLE`，组成缩小为 `BLOCKED_WRITTEN_PERMISSION + BLOCKED_TECHNICAL_EVIDENCE`。F1A 当时只取得“可作为量化数据来源”的官方书面证据和用户个人实现授权，未取得三项逐条回复，因此 F1A/F1B/F1C 当时仍是同样两类阻断。该历史决策不被删除；F1D 收到 `TS-WP-002` 后的当前状态由决策 127—130 取代。

111. **B1 不改变正式门禁和后续阶段边界**：当前继续保持 `FREE_PRODUCT_PREVIEW_GATE=PASS`、`FREE_PROVIDER_VALIDATION_GATE=BLOCKED`、`PAID_PROVIDER_UPGRADE_DECISION=PENDING`、`IFIND_TRIAL_ACTIVATION_GATE=BLOCKED`。Tushare 累计真实业务请求为 10，本治理阶段新增 Provider 调用为 0，iFinD 调用为 0；正常业务库 V13 未执行，F1/F2B/F3、Day 002、scheduler、3A-R3B-1、3B、真实交易和自动交易均未启动或授权。

112. **Tushare 官方书面回复只验证量化数据来源用途，有限实现由用户单独授权**：`2026-07-30`，Tushare 官方企业微信精确脱敏文字为“问：这个可以用来当量化数据来源吧；答：可以”。仓库不提交原始截图，也不记录联系人、微信 ID、头像、手机号、账号、Token 或其他个人信息；错误转录生成的 SHA-256 已删除。据此固定 `WRITTEN_QUANT_DATA_SOURCE_USE_PERMISSION=VERIFIED`，而 `WRITTEN_PERSONAL_LOCAL_STORAGE_PERMISSION/WRITTEN_PERSONAL_BACKTEST_PERMISSION/WRITTEN_PERSONAL_AGENT_ANALYSIS_PERMISSION` 均为 `UNVERIFIED`。用户单独固定 `USER_PERSONAL_USE_IMPLEMENTATION_AUTHORIZATION=CONFIRMED`、`F1_LIMITED_PERSONAL_USE_IMPLEMENTATION=APPROVED_BY_USER`，且不得转售、分发原始数据、共享 Token/账号或商业化数据服务。不采用 `USER_PROVIDER_PERMISSION_RISK_ACCEPTANCE`，也不把用户授权描述成 Provider 逐项许可；`POST_EXPIRY_DATA_RETENTION_PERMISSION=UNVERIFIED`、`RAW_DATA_REDISTRIBUTION_PERMISSION=NOT_GRANTED`、`TRACK_B_FULL_EVIDENCE_PROBE_STATUS=PARTIAL_NOT_COMPLETE` 保持不变。

113. **F1A Gateway 支持五 Endpoint，但只有三类可经专用类型化授权进入 V13 事实**：F1A 允许真实 `daily`、`adj_factor` 和 SSE/SZSE `trade_cal` 进入 Provider 中立 DTO；通用捕获继续拒绝 FORMAL，只有 `captureAuthorizedLimitedPersonalFormal(...)` 将 capability/licensing 映射为 `LimitedPersonalFormalCaptureAuthorization` 并严格验证 Tushare Provider、Adapter、有限个人范围、授权、禁止再分发、`RESEARCH_ONLY/formalEligible=false/SYSTEM_KNOWLEDGE_ONLY` 及三类事实白名单后，才形成 `PROVIDER_CAPTURE` 前向系统知识事实。来源身份分别为 `TUSHARE:SECURITY:<ts_code>`、`TUSHARE:ADJ_FACTOR:<ts_code>` 和 `TUSHARE:TRADE_CAL:<exchange>`。Gateway 同时支持 `stock_basic` 普通身份 DTO 和 `dividend` 部分证据 DTO，但前者不证明永久身份，后者不生成 `CORPORATE_ACTION_OBSERVATION_V1`。F1A 不跨 Provider 拼接，不声明 `V13_LINEAGE_VERIFIED`、`PROVIDER_PIT_VERIFIED`、`STABLE_SECURITY_ID_VERIFIED` 或完整公司行动闭环。生产默认 `DISABLED`，不提供 Controller、scheduler 或自动抓取入口，正常业务库不执行 V13。F1A 当时 capability 明示 `providerWrittenPermissionComplete=false`；F1D 后续只更新当前许可投影，不改变 `fullF1EntryReady=false` 或运行边界。

114. **Tushare F1A 使用单进程共享分钟/每日限流、显式会话总预算和 Endpoint 分层范围**：官方套餐总表记录 `GENERAL_2000_POINT_RATE_LIMIT_PER_MINUTE=200`、`GENERAL_2000_POINT_DAILY_LIMIT_PER_API=100000`；接口页另列 `STOCK_BASIC_OFFICIAL_RATE_LIMIT_PER_MINUTE=50`、`DAILY_OFFICIAL_RATE_LIMIT_PER_MINUTE=500`，因此不能把总表值当成所有 Endpoint 的唯一上限。应用现有安全值为 `180` 次/分钟、每 API `90000` 次/日。当前能力只声明 `processWide/sharedAcrossEndpoints/sharedAcrossCallersInProcess=true`，明确 `tokenLevelGlobalAcrossProcesses/distributedRateLimitCoordinated=false`、`dailyQuotaProcessWideOnly=true/distributedDailyQuotaCoordinated=false`。每日安全额度耗尽立即返回 `TUSHARE_DAILY_API_BUDGET_EXHAUSTED`；多实例或 scheduler 前必须另行实现分布式协调。联网还必须是显式 `MANUAL_BOUNDED` 会话，五 Endpoint 共用 10 次总预算，第 11 次在 HTTP 前返回 `TUSHARE_REQUEST_BUDGET_EXHAUSTED`。`daily/adj_factor/trade_cal` 受固定两日范围约束；无日期参数的 `stock_basic/dividend` 只受固定证券、Endpoint、共享预算和 DTO 前 1/1000 行硬上限，超限返回 `TUSHARE_REFERENCE_ROW_LIMIT_EXCEEDED` 而不是截断。受控验收禁止重试；普通运行仅允许有限、可配置、默认最多 2 次的限流重试。

115. **F1A 有限实现获批与完整 F1 准入严格拆分（历史时点）**：量化数据来源用途书面证据、用户个人实现授权和 2000 积分成本允许 `F1_LIMITED_PERSONAL_USE_IMPLEMENTATION=APPROVED_BY_USER`，但 F1A 当时完整 F1 固定为 `F1_ENTRY_READINESS=BLOCKED_MULTIPLE`，精确包含书面许可和技术证据两类阻断。F1D 后续收到逐条书面回复，只解除当前书面许可阻断；`V13_LINEAGE_PARTIAL`、`PIT_PARTIAL`、稳定证券 ID `PARTIAL` 以及生产、正常业务库、scheduler、Shadow、F2B/F3 和交易限制不变。

116. **F1A 受控联调不改变四项正式门禁**：F1A 初始只在固定两证券、固定两日执行 raw/factor/calendar 精确 6 请求；复验修复只使用剩余 4 次验证两证券的 stock_basic/dividend，其中 dividend 的 51/53 行是历史部分证据，不是两日数据，也不进入 V13 公司行动表。全部零重试，阶段总预算精确为 10；与 B1 累计后 Tushare 真实业务请求为 20，iFinD 为 0。当前继续保持 `FREE_PRODUCT_PREVIEW_GATE=PASS`、`FREE_PROVIDER_VALIDATION_GATE=BLOCKED`、`PAID_PROVIDER_UPGRADE_DECISION=PENDING`、`IFIND_TRIAL_ACTIVATION_GATE=BLOCKED`；正常业务库未访问，V13 未执行。

117. **Tushare 2000 积分技术路线固定为 REDUCED_RESEARCH_ONLY**：F1B 于 `2026-07-30` 只读取 Tushare 官方 `stock_basic/stock_company/namechange/bak_basic/adj_factor/pro_bar/dividend/trade_cal`、积分权限和 ChangeLog 页面，并结合已经验收的 B1/F1A 最小实样证据。raw、factor、SSE/SZSE calendar 和请求结束日锚定的研究级 QFQ 公式满足缩减合同；公司行动、Provider revision/旧版本、永久证券身份和全历史 `DAILY_EXACT` 不满足完整合同。因此固定 `TUSHARE_TECHNICAL_ROUTE_DECISION=REDUCED_RESEARCH_ONLY`、`TUSHARE_REDUCED_RESEARCH_CONTRACT=READY`、`fullTechnicalContractReady=false`、`reducedResearchContractReady=true`。其中 `READY` 只表示合同定义完成。判定由 `TechnicalClaim(status,evidenceIds)` 驱动，八种 action 各自需要独立证据，稳定 action ID、factor/action、Provider PIT 等强结论不得由泛化证据或裸布尔值升级。

118. **缩减研究 QFQ 公式已验证，但运行链仍为 PARTIAL**：公式固定为 `raw × trade-date factor ÷ requested-end-date factor`；锚点只能是请求结束日因子，不能随当前系统日期漂移。`QfqAsOfEngine` 与缩减合同校验器共同复用唯一 `QfqPriceMath`，相同 raw/factor/anchor 确定性复现，锚点或逐日因子缺失、因子非正、raw 晚于锚点、重复日期或跨 Provider 时安全拒绝；`dividend` 不生成、修复或反推因子。现有权威引擎的 cutoff、lineage、四位小数和舍入规则及 18 个黄金向量不变，因子变化而缺公司行动 lineage 时继续返回 `PIT_CORPORATE_ACTION_LINEAGE_UNAVAILABLE`。因此固定 `QFQ_FORMULA_QUALIFICATION=VERIFIED`、`QFQ_OPERATIONAL_RUNTIME_QUALIFICATION=PARTIAL`、`REDUCED_RESEARCH_RUNTIME_READY=false`；Tushare factor 继续为 `SYSTEM_KNOWLEDGE_ONLY`。

119. **Tushare 身份、公司行动和 ChangeLog 不得越级解释**：`stock_basic` 只支持当前普通证券字段；`stock_company.com_id` 只可作为 `ISSUER_IDENTITY_EVIDENCE`；`namechange` 只可作为 `SECURITY_NAME_HISTORY_EVIDENCE`；`bak_basic` 正式权限要求 5000 积分，当前 2000 积分固定为 `HISTORICAL_SECURITY_LIST_PERMISSION_INSUFFICIENT`。`dividend` 的现金分红、送股和转增均为 `PARTIAL`，配股、拆股、并股、更正和撤回为 `NOT_SUPPORTED`；没有稳定 action ID、revision、逐条 Provider 发布时间、更正/撤回链或 factor/action 稳定解释关系。官方 ChangeLog 只能证明接口/字段演进，不能证明单条历史事实 revision 或旧版本可查。因此继续保持 `V13_LINEAGE_PARTIAL`、`PIT_PARTIAL`、`STABLE_SECURITY_ID=PARTIAL`。

120. **F1B 的缩减合同 READY 不等于缩减运行 READY、完整 F1 READY 或后续阶段授权（历史时点）**：F1B 当时完整 F1 为 `F1_ENTRY_READINESS=BLOCKED_MULTIPLE`，精确包含书面许可与技术证据两类阻断。F1B 的缩减合同只允许后续独立授权阶段规划手工有界 raw/factor/calendar、研究级 QFQ、前向 `SYSTEM_KNOWLEDGE_PIT`、普通身份和 dividend 解释证据；该阶段结束时没有缩减运行入口。不允许完整公司行动、Provider PIT、历史 revision、永久身份、跨 Provider QFQ、正常业务库、scheduler、全市场采集、Shadow、Day 002、F2B、F3、3A-R3B-1、3B 或交易。F1D 只解除当前书面许可阻断，不改变本决策的技术和运行限制。

121. **F1B 当时把接口页频次优先纳入保守较小值合同，但尚未实现 Endpoint 级限流**：官方套餐总表的 200 次/分钟和每 API 100000 次/日，与 `stock_basic` 接口页 50 次/分钟、`daily` 接口页 500 次/分钟共同形成 `OFFICIAL_ENDPOINT_RATE_LIMITS=PARTIAL_CONFLICT_IDENTIFIED`。存在多个适用上限时，运行实现必须使用最保守的较小值。F1A 固定 10 次会话仍安全，但 F1B 当时的单进程共享 180 次/分钟限流不能证明 `stock_basic` 长期摄取安全，故该阶段结束时 `ENDPOINT_SPECIFIC_RATE_LIMIT_ENFORCED=false` 且 `REDUCED_RESEARCH_RUNTIME_READY=false`。F1B 不修改限流器；后续 F1C 的实施状态由下列新决策记录。

122. **F1C 实施五 Endpoint 保守较小值限流，并把运行资格绑定到 Gateway 的实际策略与 limiter**：类型化 `TushareEndpointRateLimitPolicy` 只接受 `stock_basic/daily/adj_factor/trade_cal/dividend`，安全上限固定为 `45/180/180/180/180` 次/分钟；所有 Endpoint 还共享进程内 180 次/分钟窗口，每 Endpoint 每 Asia/Shanghai 自然日最多 90000 次。全局分钟、Endpoint 分钟和 Endpoint 每日额度在同一临界区原子检查并同时登记，未知 Endpoint 不回落通用值，重试同样计数。`F1cRateLimitedGateway` 必须从实际注入的 policy 与 limiter 快照生成类型化合同；普通无界 Gateway 在首个 Provider 调用前拒绝，合成 Gateway 也必须经过冻结策略和真实或等价 limiter。因此 `ENDPOINT_SPECIFIC_RATE_LIMIT_ENFORCED=true`、`CONSERVATIVE_ENDPOINT_MINIMUM_POLICY_ENFORCED=true`；但 `OFFICIAL_ENDPOINT_RATE_LIMITS=PARTIAL_CONFLICT_IDENTIFIED` 不变，`distributedCoordination=false`，多进程或多实例启用前仍需独立协调。

123. **F1C 运行授权只允许随机隔离的单证券三请求手工模式**：唯一授权工厂固定 Tushare Pro V1 Adapter、`LIMITED_PERSONAL_RESEARCH_USE/ISOLATED_MANUAL/FORMAL/RESEARCH_ONLY/formalEligible=false`、单证券、最多两自然日、`daily/adj_factor/trade_cal` 三次、零重试。正常业务数据库、scheduler、Shadow、Agent 决策、回测执行、投资建议和交易全部禁止；伪造 Provider/Adapter、放宽预算或允许 public 的授权在 Provider 调用前拒绝。

124. **F1C 守卫绑定专用数据库身份与实际捕获事务，不能只凭安全 Schema 名授权**：Provider 前的预检和捕获事务内的前后校验都要求显式用途 `F1C_ISOLATED_RESEARCH`、专用本地隔离测试库的 `current_database()`/`current_user`/JDBC URL、`f1c_tushare_research_<32位十六进制随机后缀>`、无 public/其他回退的 search_path 及精确 V1—V13。写入只允许 Spring 代理上的 `captureAuthorizedF1cIsolatedReducedResearch(...)`，其前后守卫必须使用事务绑定的同一 JDBC 连接并保持相同 PostgreSQL backend PID；`CaptureResult` 还必须完整且 `appended + idempotent = received = expected`。任一后置检查失败即回滚整个事务，不保留 batch 或观察。正常业务数据库即使创建同名安全 Schema 也拒绝；通用 FORMAL 捕获和 F1A 既有入口不放宽。只保存 raw/factor/calendar；缩减 QFQ、stock_basic、dividend、公司行动、Provider revision 和 Agent/投资结论不写入。

125. **隔离手工缩减 QFQ READY 与完整 QFQ/生产 READY 永久分栏**：F1C 只复用唯一 `QfqPriceMath`，以请求窗口最后开市 raw 日的同日 factor 为显式 anchor，在内存返回独立的 `REDUCED_RESEARCH_FORMULA_ONLY` 结果。因此 `QFQ_REDUCED_RESEARCH_RUNTIME_QUALIFICATION=VERIFIED`、`REDUCED_RESEARCH_ISOLATED_MANUAL_RUNTIME_READY=true`；同时 `QFQ_FULL_LINEAGE_RUNTIME_QUALIFICATION=PARTIAL`、`REDUCED_RESEARCH_RUNTIME_READY=false`、`REDUCED_RESEARCH_PRODUCTION_RUNTIME_READY=false`、`NORMAL_BUSINESS_DATABASE_RUNTIME_READY=false`。`QfqAsOfEngine` 的公司行动 lineage、factor predecessor、cutoff、用途、Provider 一致性和 18 个黄金向量不得放宽。

126. **F1C 当时不解除完整 F1、四项正式门禁或后续阶段阻断**：F1C 完成时 `F1_ENTRY_READINESS=BLOCKED_MULTIPLE` 仍由书面许可与技术证据组成；`FREE_PRODUCT_PREVIEW_GATE=PASS`、`FREE_PROVIDER_VALIDATION_GATE=BLOCKED`、`PAID_PROVIDER_UPGRADE_DECISION=PENDING`、`IFIND_TRIAL_ACTIVATION_GATE=BLOCKED` 不变。F1C 使用合成 Gateway，Provider 新增调用为 0，Tushare 累计仍为 20，iFinD 为 0；不授权正常业务库 V13、scheduler、Shadow、Day 002、F2B、F3、3A-R3B-1、3B 或交易。F1D 后续只改变当前书面许可/F1 blocker 聚合，不回写 F1C 历史。

127. **TS-WP-002 以用户提供的官方精确文字转录登记，不冒充原件审阅或独立认证**：`2026-07-31T11:07:00+08:00` 收到七项脱敏文字，内容逐项允许个人 Tushare Pro 2000 积分账号本地数据库保存、回测/历史回放、本地 AI/Agent、程序自动调用/定时更新、字段结构/Hash/摘要/错误日志留存及持续本地保存。`OFFICIAL_REPLY_AT=UNKNOWN`，用户证明来源为 Tushare 官方回复；`ORIGINAL_ARTIFACT_STORED/SCREENSHOT_REVIEWED/INDEPENDENT_SOURCE_AUTHENTICITY_REVIEWED=false`。仓库不保存截图、联系人、微信 ID、头像、手机号、账号或 Token。

128. **Tushare 个人研究书面许可由类型化 evidence claim 闭环，但禁止用途不扩张**：`TushareWrittenPermissionQualification` 要求每项 `VERIFIED` 必须有登记证据。TS-WP-001 支持量化数据来源，TS-WP-002 支持本地保存、回测/回放、Agent、自动更新、技术审计元数据留存、到期后继续本地保存及个人 2000 积分账号范围。当前 `personalResearchPermissionComplete=true`、`providerWrittenPermissionComplete=true`、`WRITTEN_PERMISSION_GATE=PASS`；原始数据再分发、商业数据服务和 Token/账号共享均为 `NOT_GRANTED`。

129. **当前完整 F1 从双阻断收敛为单一技术证据阻断**：`TushareF1EntryQualification` 只从书面许可和技术资格两个类型化模型推导门禁。当前 `WRITTEN_PERMISSION_GATE=PASS`、`TECHNICAL_EVIDENCE_GATE=BLOCKED`、`F1_ENTRY_READINESS=BLOCKED_TECHNICAL_EVIDENCE`，唯一粗粒度 blocker 为 `BLOCKED_TECHNICAL_EVIDENCE`。完整公司行动、稳定 action ID、factor/action、revision/历史版本、永久证券身份、全历史 `DAILY_EXACT` 与完整 QFQ lineage 仍缺证，因此 `fullF1EntryReady=false`、`V13_LINEAGE_PARTIAL`、`PIT_PARTIAL`、稳定证券 ID `PARTIAL` 不变。

130. **书面许可 PASS 不开放任何生产或后续运行入口**：Capability 从类型化许可/F1 模型投影，但继续保持 `formalEligible=false`、`reducedResearchProductionRuntimeReady=false`、`normalBusinessDatabaseRuntimeReady=false`、`schedulerRuntimeReady=false`、Agent/回测/F2B/F3 runtime 均为 false。F1C 隔离手工运行 READY 仍只适用于随机隔离 Schema。B1 的 `PARTIAL_NOT_COMPLETE/NOT_MET` 与两项事前书面许可 `UNVERIFIED` 是执行时点历史事实，不被 F1D 追认或重跑。四项正式门禁保持 `PASS/BLOCKED/PENDING/BLOCKED`。

131. **TS-WP-002 后重新计算 Tushare 路线评分但不改变路线排名**：法律与用途项从历史 `2.5` 调整为 `4.0`，其余 V13/QFQ、PIT/版本、覆盖/稳定、个人成本和接入复杂度保持 `3.5/2.0/3.5/4.5/4.0`。加权结果为 `4.0×30% + 3.5×25% + 2.0×15% + 3.5×15% + 4.5×10% + 4.0×5% = 3.55`。不记满分是因为再分发、商业数据服务和 Token/账号共享仍不授权；技术和 PIT 分数不因许可闭环上调。主要路线仍为 Tushare，备用仍为 iFinD。

132. **F1D 书面许可闭环已经验收合入，但完整 F1 仍只有技术证据阻断**：F1D 三提交链 `349856ea6e9e3dc423fc1ad9115886cfc8858159` → `049c750026fa00dad70c12667fad732af07d60ce` → `01024df465afcfa34dfd4efdbef7d56d32419aa1` 已通过 ChatGPT 对实际 Git 的最终复验并经用户批准纯 fast-forward 合入。类型化模型同时验证证据 provenance、Claim 主题和 Evidence 支持范围，TS-WP-001 只支持量化数据来源，TS-WP-002 只支持已登记的七项个人用途；不允许跨主题借证。当前 `WRITTEN_PERMISSION_GATE=PASS`、`F1_ENTRY_READINESS=BLOCKED_TECHNICAL_EVIDENCE`，唯一粗粒度阻断是 `BLOCKED_TECHNICAL_EVIDENCE`。原始数据再分发、商业数据服务和 Token/账号共享仍为 `NOT_GRANTED`，生产和后续运行能力不因许可 PASS 自动开放。

133. **F1E 专用本地研究路线只把实现和受控验收合同设为 READY**：准入决策固定为 `REDUCED_RESEARCH_ROUTE_DECISION=DEDICATED_LOCAL_RESEARCH_PATH`。F1E 可以把 `REDUCED_RESEARCH_LOCAL_RUNTIME_IMPLEMENTATION_READY` 与 `REDUCED_RESEARCH_CONTROLLED_ACCEPTANCE_READY` 设为 true，但在真实受控验收由用户另行接受前，`REDUCED_RESEARCH_OPERATIONAL_READY=false`。这三个状态不得合并为模糊的“运行 READY”，也不改变 `fullF1EntryReady=false`、`fullTechnicalContractReady=false` 或 `formalEligible=false`。

134. **F1E 持久化只允许专用数据库身份，安全 Schema 名本身不足以授权**：唯一目标固定为本机数据库 `stock_quant_research`、用户 `stock_quant_research`、用途 `TUSHARE_DEDICATED_LOCAL_RESEARCH`、唯一 Schema/search path `tushare_research` 和精确 V1—V13。Provider 调用前只读预检，捕获事务前后必须在同一事务连接上重验数据库身份、Schema、search path、迁移和 backend PID。public、正常业务库、通用测试库、其他 Schema、public 回退、迁移不完整或目标变化必须拒绝；不得修改 Flyway V1—V13 或 Repository SQL。

135. **F1E 批次采用单日 1—3 证券、共享 3×N 请求预算和全有或全无捕获**：唯一授权工厂只允许个人 2000 积分、`RESEARCH_ONLY/DEDICATED_LOCAL_MANUAL`、SSE/SZSE、一个自然日和 1—3 只不重复证券。每只证券只调用 `daily/adj_factor/trade_cal`，一个批次共用同一会话，精确消耗 3/6/9 次，最大 9，重试为 0。必须先验证全部响应与 limiter/session 计数，再以一个 Spring 事务捕获全部 raw/factor/calendar；任一 Provider、响应、闭合、守卫或写入失败都整体回滚，不能保留部分证券事实。

136. **F1E QFQ 仍是内存公式级降级结果，不替代权威完整 lineage 引擎**：专用运行只调用共享 `QfqPriceMath`，返回 `REDUCED_RESEARCH_FORMULA_ONLY` 和 `systemKnowledgeOnly=true`；Provider PIT、公司行动 lineage、永久证券身份、完整 QFQ、生产、Agent、回测、投资建议及交易资格都为 false。QFQ 结果、stock_basic、dividend、公司行动和 Provider revision 不写入；`QfqAsOfEngine` 的公司行动 lineage、factor predecessor、cutoff、用途与 Provider 一致性门禁以及黄金向量不得放宽。

137. **F1E 不改变完整 F1、正式门禁或后续阶段**：当前继续保持 `F1_ENTRY_READINESS=BLOCKED_TECHNICAL_EVIDENCE`、`V13_LINEAGE_PARTIAL`、`PIT_PARTIAL` 和稳定证券 ID `PARTIAL`。`FREE_PRODUCT_PREVIEW_GATE=PASS`、`FREE_PROVIDER_VALIDATION_GATE=BLOCKED`、`PAID_PROVIDER_UPGRADE_DECISION=PENDING`、`IFIND_TRIAL_ACTIVATION_GATE=BLOCKED` 不变。生产、正常业务库、scheduler、Agent、回测、Shadow、Day 002、F2B、F3、3A-R3B-1、3B 和交易都不启动；F1E 使用合成 Gateway，新增 Provider 调用为 0，Tushare 累计真实业务请求仍为 20，iFinD 为 0。

138. **F1F-A 将受控验收从固定状态改为不可伪造的类型化证据投影**：默认状态为 `NOT_RUN`，并可明确表达 `CANDIDATE/PASSED/FAILED/STALE/INCOMPATIBLE_BASELINE`。授权必须一次性绑定代码基线、Tushare、一个证券、一个交易日、`daily/adj_factor/trade_cal`、三次调用、零重试、专用数据库/用户/Schema/V13和有效期。F1F-A 的离线成功只能生成 `CANDIDATE`，没有公开 `PASSED` 构造入口；真实 `PASSED` 必须留给用户另行批准的 F1F-B。

139. **缩减研究 operational 验收独立于完整 F1 技术资格和正式门禁**：未来只有真实受控证据满足全部数据库、原子写入、SYSTEM_KNOWLEDGE、formula-only QFQ、无敏感输出和零禁止阶段条件时，才能讨论 `REDUCED_RESEARCH_OPERATIONAL_READY=true`。该投影不得改变 `F1_ENTRY_READINESS=BLOCKED_TECHNICAL_EVIDENCE`、完整技术合同、生产/正常业务库、scheduler、Agent、回测、Shadow、F2B/F3、交易或四项正式门禁。

140. **F1F-A 候选证据不得冒充 F1F-B 强证明**：当前 CAS 只保护同一内存授权对象，配置值只证明声明的完整哈希相等，`observedAt` 只证明捕获调用使用同一微秒 `Instant`，敏感输出尚未由独立执行审计证明。因此候选分别记录 `OBJECT_INSTANCE_CAS_ONLY`、`CONFIG_DECLARED_EXACT_MATCH`、`databaseReadbackVerified=false` 和 `NOT_ATTESTED`。未来 `PASSED` 必须额外取得持久化 acceptance ID 唯一消费、最终构建产物完整哈希、数据库时间回读以及敏感输出审计，不能直接复用候选字段升级。

141. **F1F-B1 将强证明实现为独立、默认不加载的治理执行机制**：acceptance ID 由独立 V14 治理表和 SQL 单向转换约束跨线程/JVM/进程唯一消费；正常应用 Flyway 主历史仍只加载 V1—V13。V14 必须使用独立 location 和 `flyway_controlled_acceptance_history`，baseline 13 后显式迁移，并由 Java 前后守卫与脚本内数据库/Schema/主历史/治理历史检查共同拒绝误加载。`RUNNING` 必须在 Provider 前独立提交，崩溃恢复只封存已预占执行为 `INTERRUPTED`，不得消费未预占授权、重试或复用原 ID。

142. **构建、输出、回读和 PASSED 均以实际证据为准**：受控构建只允许在冻结集成分支且本地/远程集成 SHA 一致时生成，MANIFEST 与相邻 sidecar 必须交叉匹配并由运行时重算当前 executor JAR SHA-256；TEST proof 不具治理资格。输出隔离必须先于敏感材料 supplier，独占捕获并恢复 stdout/stderr、当前 Logback 拓扑和异常链，只持久化类别与位置。SYSTEM_KNOWLEDGE 必须在 F1E 捕获事务提交后、无活动事务时回读 V13 envelope 与 raw/factor/calendar typed 表并重新验证数据库身份；捕获与回读 PID 分别留证。数据库中的 `PASSED` 字符串不能绕过严格 JSON、证据摘要、精确状态历史、构建来源和规则版本重验，且持久化 PASSED 后必须重新加载再次验证。

- **F1F-B1 信任边界**：JAR/sidecar SHA-256 和数据库 evidence digest 用于检测意外变化与普通应用路径篡改，不是外部根密钥签名；能够同时改写 Git/JAR/sidecar 或完整数据库行、转换历史和证据的特权管理员超出本阶段防护范围。输出审计覆盖边界内 stdout/stderr、当前 Logback 和同步完成的异常/线程输出，不证明任意外部文件、未桥接日志框架或边界结束后的脱离线程无泄漏。F1F-B2 必须使用最小专用进程、专用最小权限账号、输出白名单、无未等待后台任务和用户一次性授权。

143. **F1F-B1 离线成功不改变治理状态**：`TEST` 来源永远只得到 `SUCCEEDED_CANDIDATE`。本阶段没有调用 Provider、没有执行 F1F-B2，继续保持 `CONTROLLED_ACCEPTANCE_STATUS=NOT_RUN`、`REDUCED_RESEARCH_OPERATIONAL_READY=false`、`F1_ENTRY_READINESS=BLOCKED_TECHNICAL_EVIDENCE` 及四项正式门禁。

144. **F1F-B1 已验收合入，但 B2-PRE 的启动审计独立判为 NOT_READY**：B1 双提交链已经纯 fast-forward 合入 `e3777602fadd65f3af0a2ba8ac6e886693d745d5`。B2-PRE 认可其持久化状态机、回读与强证明基础，但确认真实启动还缺专用最小进程、覆盖秘密/数据源初始化前的输出审计、显式 V14 bootstrap、默认非正式构建模式和 Maven Wrapper 冻结。因此 `e3777602...` 只作为 B2-RUNNER 开发父提交，不再作为未来真实验收冻结 SHA。

145. **正式受控构建与准备演练严格分离**：构建模式只有 `PREPARATION_ONLY` 与 `CONTROLLED_BUILD_ARTIFACT`，默认必须是前者。两者都使用 Maven Wrapper `3.9.16` 并绑定本地/远程 Git、MANIFEST、sidecar 和 JAR SHA-256；只有显式正式模式、冻结集成分支和本地/远程同 SHA 才可能取得治理资格。准备产物即使改名或篡改 sidecar 模式，也不能生成真实授权、`REAL_CONTROLLED_ACCEPTANCE`、`PASSED` 或 operational 投影。

146. **F1F-B2 必须由非 Spring 一次性最小进程执行，并采用显式受守卫治理初始化**：专用 runner 不启动普通 Spring Boot、Web、Controller、scheduler 或任何 Agent/回测/Shadow/交易入口；组件只按白名单手工装配。输出审计必须先于构建/授权解析、秘密读取、DataSource、Flyway 和 Provider 客户端。治理 V14 固定 `baselineOnMigrate(false)`；只有构建、授权、数据库身份、search path 与主历史 V1—V13 全部通过后，才允许显式 baseline 13 和迁移 V14。该机制完成不改变 `CONTROLLED_ACCEPTANCE_STATUS=NOT_RUN`、`REDUCED_RESEARCH_OPERATIONAL_READY=false`、完整 F1 技术阻断或四项正式门禁。

147. **F1F-B2-RUNNER 的正式执行产物必须由单一专用 JAR 决定入口并从隔离 commit 构建**：构建脚本从核验 commit 的 `git archive` 在系统临时目录离线构建，不使用共享工作区产物；MANIFEST 固定不可由 `loader.main` 覆盖的 `JarLauncher` 和 Runner `Start-Class`，运行时只接受单一真实 JAR classpath。Maven 构建 JVM、实际启动 JVM、MANIFEST 与 sidecar 的 `java.version` 必须一致；主 Flyway history 必须精确 V1—V13 且无失败，独立治理 history 必须精确 BASELINE/13 与 SQL/V14 的类型和脚本。上述加固不签发正式证明或授权，也不改变任何门禁。

148. **F1F-B2-RUNNER 四提交链已验收合入，但真实验收仍为 NOT_RUN**：`4c136b32` → `0d8109cc` → `e18623f4` → `213264bc63a2584f0fbb30dca059abf272e62a64` 已通过实际 Git 复验并纯 fast-forward 合入。该合入只提供执行机制，不等于签发正式构建证明、授权、acceptance ID 或消费三次 Provider 预算；`CONTROLLED_ACCEPTANCE_STATUS=NOT_RUN` 与 `REDUCED_RESEARCH_OPERATIONAL_READY=false` 不变。

149. **首次 B2-FREEZE 为 NOT_READY，旧草案永久废弃**：`2026-08-03` 基于 `213264bc...` 的清单审计确认两个阻断：Runner 只接受 `AuthorizationFile`，不能接受清单中的松散构建/数据库参数；仓库缺少安全创建全新专用数据库并只执行主 V1—V13 的入口。旧草案 ID `F1FB2_20260803_140506_96C6DFB7` 不得写库、授权、入证或复用。

150. **专用数据库准备必须使用一次性非 Spring、全新专用实例和主 V1—V13 白名单**：固定目标为 `127.0.0.1`、数据库/用户 `stock_quant_research`、Schema/search path `tushare_research` 与显式端口。目标数据库、角色或其他业务数据库存在即在 DDL 前拒绝。输出审计先于秘密；管理员秘密在最终专用用户密码读取前清零；Flyway 只扫描 `classpath:db/migration`，禁止 baseline、repair、clean、治理 location 和 V14。部分失败不自动删除目标，只标记 `INCOMPLETE_NOT_APPROVED` 等待人工处置。

151. **F1F-B2 正式 Runner 只接受严格单一 AuthorizationFile**：授权文件必须绑定最终冻结 SHA、JAR SHA-256、构建证明路径、固定数据库身份与端口、证券、日期、Endpoint 和三次/零重试预算，且不得包含 Token 或密码。重复、未知、缺失、空白或类型错误字段全部拒绝；Runner 不增加任何松散参数。DBPREP 合入后必须用新的集成 SHA 重跑简化 FREEZE，不得沿用旧 SHA、JAR、sidecar 或授权草案。

152. **首次真实 F1F-B2 的数据库守卫失败是安全失败，不能提升 operational**：ID `F1FB2_20260803_REISSUE_C5A3D7B92A94` 在 `2026-08-03` 按冻结顺序完成 3 次 Provider 调用且零重试，治理 V14 已完成；捕获前因 `TUSHARE_DEDICATED_RESEARCH_TRANSACTION_REQUIRED` 被拒绝，`capture_batch_id=NULL`，ID 永久封存。Tushare 累计真实业务请求更新为 23。该结果固定为 `CONTROLLED_ACCEPTANCE_STATUS=FAILED_DATABASE_GUARD`，`REDUCED_RESEARCH_OPERATIONAL_READY=false`，不允许补跑或把 Provider 响应成功替代原子持久化证据。

153. **非 Spring Runner 的 F1E 捕获必须使用专用 DataSource 的显式事务**：Runner 的手工白名单装配不依赖 Spring 代理，因此捕获服务不能只依赖方法上的 `@Transactional`。F1E 专用捕获必须通过构造时注入的 `PlatformTransactionManager` 显式执行；活动事务必须在首次 Repository 写入前绑定守卫所核对的同一专用 DataSource，并覆盖 Temporal dataset、capture batch、observation 与全部 typed fact。错误事务管理器、无事务或绑定错误 DataSource 继续返回既有守卫 reason；任一事实写入失败整批回滚，不得伪造事务状态或放宽守卫。

154. **受控验收回读必须区分批次证券 identity 与 typed fact identity**：批次继续使用 `TUSHARE:SECURITY:<ts_code>`；raw daily 使用证券 identity，adjustment factor 使用 `TUSHARE:ADJ_FACTOR:<ts_code>`，trading calendar 使用 `TUSHARE:TRADE_CAL:<exchange>`。V13 的批次关联只来自 `pit_market_fact_observations.batch_id`，三类 typed 表只通过 `observation_id` 关联，不得假设 typed 表存在 `batch_id`，也不得用一个批次 identity 覆盖三种事实。ID `F1FB2_20260803_POSTFIX_EB61FFB9663C` 因违反此回读语义而在 3 次真实调用、零重试后停于 `FAILED_VALIDATION` 并永久封存；Tushare 累计真实业务请求为 26。该修复不改变事务守卫、完整性数量、`F1_ENTRY_READINESS=BLOCKED_TECHNICAL_EVIDENCE`、`REDUCED_RESEARCH_OPERATIONAL_READY=false` 或四项正式门禁。

155. **当前批次必须以不可变事实引用证明幂等复用的 SYSTEM_KNOWLEDGE，而不是强制生成重复 observation**：ID `F1FB2_20260804_READBACK_POSTFIX_D6D0F13C478D` 在事务与三类 typed identity 回读通过后，因 `TUSHARE_CONTROLLED_ACCEPTANCE_SYSTEM_KNOWLEDGE_READBACK_INVALID` 停于 `FAILED_VALIDATION` 并永久封存；Provider 调用 3、重试 0，Tushare 累计真实业务请求为 29。V13 明确把与当前链尾 canonical content hash 相同的事实视为幂等，因此新 capture batch 可以合法地有 0 条新 observation。批次必须在现有 metadata 中冻结每种事实的 type、类型化 source identity、natural key 和 canonical hash；提交后回读逐项解析这些当前批次引用并核对链尾及 typed fact，不能把“当前 batch_id 下必须有三条新 observation”作为唯一证据，也不能重写旧 observation 的首次观察时间。时间比较统一采用 UTC `Instant` 并规范化到 PostgreSQL 微秒精度，不使用宽泛容差。写事务前后 backend PID 必须相同；提交后 readback 可使用新连接和不同 PID，仍须重新验证数据库身份。该修复不改变 typed identity、事务/回滚、完整 F1 技术阻断、operational 状态或四项正式门禁。
