# Stock Quant Pro 1.3.1

## 修复内容

- 修复智能选股页面在读取旧扫描任务时表格为空。
- 兼容 PostgreSQL JSONB 返回的 `PGobject` 结构。
- 历史任务可正常展示原始排名结果。
- 历史任务不再错误启用严格候选过滤。
- 旧结果中的技术指标自动从 `metrics` 字段回填。

## 升级说明

保留 `.env` 和数据库，用本版本覆盖代码后重新加载 Maven、重启 Java 和前端即可。
本版本不新增数据库迁移脚本。
